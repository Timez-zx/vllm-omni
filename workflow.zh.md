# 我们给 vllm-omni 加了什么

目标:让一个人能开着摄像头和麦克风,和模型**连续聊很久**,而且每次回答都很快。

原版能做到"聊几轮",但聊久了会出问题。下面按加入顺序,一个改动一节,每节都是:
**场景 → 原来会怎样 → 现在怎样**。

代码就在本仓库,分支 `live-agent`。新能力默认都是**关着的** —— 不配置的话行为和原版一样,**只有一个例外**:第十节改的是"一个模型阶段交给另一个阶段什么",它**默认是开的**,用 `VLLM_OMNI_TALKER_TEXT_ONLY=0` 关掉。

---

## 改完之后,能做什么

| 能力 | 一句话 |
|---|---|
| 长会话不再重复劳动 | 聊到第 20 轮,只处理新画面,不再把前 19 轮重看一遍 |
| 画面自动变小 | 手机传 1280×720,服务端自己缩到 640×352 再给模型 |
| 取帧节奏可控 | 画面静止时不会"看不见现在",画面乱动时不会把提示塞满 |
| 对话可以无限继续 | 快到上限时自动换一个底层请求,用户察觉不到 |
| 撞上限之前有预警 | 每轮打印距离上限还有多远,以前完全看不见 |
| 出故障会喊 | 三类"静默卡死"现在会自己报出来,以前日志里什么都没有 |

---

## 一、聊得越久,回答越慢 —— 因为前面的话被重复算了

*2026-07-31 00:07 – 01:24 · `e3ebb356` 一个会话一个请求 · `a53a0834` 排空帧缓冲 · `fa52c693` 拒绝轮次重叠*

**场景。** 用户举着摄像头,聊了 20 轮。

**原来会怎样。** 每问一句,系统都当成一个**全新的任务**来办 —— 前面 19 轮的画面和对话,得**从头再走一遍**,才能回答第 20 句。聊得越久,每一句就越慢。

**现在怎样。** 整个对话从头到尾算**一个任务**,每轮只把**新增的那几帧和这句话**追加进去。前面的东西一直留在显存里,不用重算。

**效果。** 语音那一段几乎不再因为"聊得久"而变慢。原来聊到 3 万多字的上下文时,是最慢的时候;现在反而比"把历史全丢掉"的做法更快 —— **留着历史比丢掉更便宜**。

---

## 二、手机传过来的画面太大了

*2026-07-31 00:01 / 00:37 · `d1abbf8f` 到达即降采样 · `6ec99006` 挪出音频通道*

**场景。** 客户端摄像头是 1280×720,每一帧都原样传给模型。

**原来会怎样。** 模型按原尺寸处理每一帧。一帧 720p 的画面要拆成 880 个"块"来算;而这些块的数量直接决定了要花多少时间。

**现在怎样。** 帧一到服务端就先**缩小**(比如缩到 640×352,变成 220 个块),然后才给后面所有环节用。长宽比保持不变,只缩不放。

**有一个坑必须避开。** 缩放必须放在**最前面** —— 判断"这帧和上一帧像不像"的过滤器、缓存、以及最终喂给模型的内容,必须看到**同一个版本**的画面。否则会出现"过滤器拿大图比较、模型读小图"这种对不上的情况。

**还有一个性能坑。** 这个缩放一开始是"顺手"做的,结果占用了**同一条负责把声音推给用户的通道** —— 于是缩图的时间直接加在别人等回答的时间上。后来把它挪到旁边单独做。

---

## 三、画面静止时"看不见现在",画面乱动时"塞满提示"

*2026-07-31 00:04 · `f4837245`*

**场景一。** 用户对着一面白墙不动,过了半分钟才问一句。
**场景二。** 用户在走动,画面每一帧都不一样。

**原来会怎样。** 系统靠"这帧和上一帧像不像"来决定要不要留。于是:
- 白墙那种情况,**可能几十秒一帧都不留**,模型对"现在"是瞎的
- 走动那种情况,**一口气留十几帧**,把提示塞满,回答变慢

**现在怎样。** 给"两次保留之间隔多少帧"加了**下界和上界**:
- 下界:再怎么变化,也不会连续猛留
- 上界:再怎么静止,隔够了就必须留一帧

判断像不像的逻辑**一点没改**,只是限制它"多久可以表态一次"。按帧数算而不是按秒,所以客户端帧率变了行为也不变。

---

## 四、聊到某一轮,对话突然停住了

*2026-07-31 02:42 · `b866eb15`*

**场景。** 聊到第 27 轮,忽然:模型还在**打字**(文本一直出),但**一句话都不说**。等了两分钟,日志里**一个错误都没有**。

**为什么。** 这是最难查的一个。负责"说话"的那一段有一个容量上限,撞上去之后:要么整个进程被打死,要么**每一轮都被悄悄跳过**,谁都不报错。

**关键在于:满的不是你以为的那个东西。**

> 那个上限装的不是"聊天历史",而是**说话那一段自己的账本** —— 里面既有每轮新增的内容,也有**它自己生成的语音数据**。而话说得多的一轮,语音数据是更大的那一块。
>
> **所以撑爆的速度,取决于"模型说了多少话",不是"聊了多少轮"。**

这解释了一个反直觉的现象:**回答简短时 50 轮没事,回答啰嗦时 27 轮就崩** —— 轮数多的那次反而更安全。

**现在怎样。**

- **每轮都打印"现在用了多少 / 上限是多少"**。以前这件事在崩掉之前**完全看不见**,没有任何现成指标会提示快到了。
- 可以设一个额度,到了就**干净地结束对话**并告诉客户端原因,而不是让某个环节崩掉或者悄悄卡住。

这只是**护栏**,不是解决 —— 触发了对话就结束了。真正的解决是下一节。

---

## 五、让对话能无限聊下去

*2026-07-31 03:46 · `4502b142`*

**场景。** 用户想一直聊,不想因为"系统内部满了"被打断。

**现在怎样。** 快到上限时,系统在背后**换一个新的底层请求**:
1. 把旧的退役
2. 开一个新的
3. 把**最近几轮的对话文字**当作开场,先说给新的听
4. 手上还没用的画面,跟着这一轮一起给它

**用户完全察觉不到**,只是那一轮回答慢一些(要重新热身)。

**换过去之后丢什么。** 只有**文字**过得去,**画面记忆过不去**。所以模型仍然看得见"现在"(手上的帧跟着过去了),丢的是**更早看到过的细节**。

**为什么"带过去的历史"必须有限。** 带过去的内容要在新请求里重新读一遍。如果不设上限,**每换一次带的东西就更多,最后光是这些开场白就又把上限撑爆了** —— 那就等于没换。所以默认只带最近 8 轮。

一句话:**时间上可以无限,记忆上必须有限。**

**效果。** 40 轮连续对话、中途换了 2 次,全程没崩没卡。同样的设置**不换的话,第 27 轮对话就终结了**。代价是换的那一轮慢约 3 倍,平摊到每轮约多几十毫秒。

---

## 六、系统明明有活干,却以为自己闲着

*2026-07-30 23:59 – 07-31 12:32 · `c27654de` 直接数真实状态(最终修复) · `b1f423f1` 待处理数据算作有活 · `1aed4032` 清掉已取消的任务 · `9fdee244`、`b7392f3c` 两次未命中的尝试*

**场景。** 换请求之后那一轮,对话又卡住了 —— 而且这次连"正在忙"的迹象都没有。

**为什么。** 系统内部有个计数,用来判断"现在还有没有活要干"。这个计数**在某些情况下会漏减**,越用越偏。而**偏一个,就正好抵消掉一个真实的任务** —— 于是系统认为"没活干",就**睡过去了**;而活其实就在手上,永远没人接。

打个比方:门口的人数牌坏了,牌上写着"里面还有 1 位没走",实际上早走了;于是新来的客人被抵消掉,值班的人以为没人,关灯下班了。

**现在怎样。** 不再相信那个计数,**每次直接去数一遍**当前真实的状态。

顺带还有两处同类问题:一个是"已经拿到手、等着处理的数据"没被算作"有活干";另一个是已经取消的任务留在队列里没清掉,被后面的逻辑捞出来算,直接把进程打死。

**一个值得记的判断。** 中间我试过"把那个坏掉的计数**修正**过来",结果更糟 —— 反而把它推成了负数。**报告别人的账本坏了是有用的,自己动手去改它不是。**

---

## 七、让故障自己开口

*2026-07-31 01:19 – 04:21 · `921314e8`、`625ad170` 卡死探测 · `53a709a5`、`241aa5fd` 状态转储 · `cd29fe65` 进出记录 · `713e5361` 空转上报*

**场景。** 前面那些卡死,共同特点是:**日志里什么都没有**。系统安静地停在那里,分不清是"正常空闲"还是"卡死了"。

**现在怎样。** 加了几组自检,平时不吭声,出事才喊:

- 某个环节**手里有任务但一直没进展** → 报出来,并把当时所有状态打印出来
- 某个环节**从"有任务"变成"一个都没有"** → 报出来(说明任务在到达它之前就丢了)
- 每次任务的**进入和结束**都记一行,结束时说明原因("正常完成"和"被取消"成因完全不同,以前长得一样)

**这里有一条经验,比任何单个 bug 都值钱。**

原来所有自检都挂在同一段循环的**末尾**。循环一停,它们**全体沉默** —— 而这种沉默读起来像"一切正常",恰恰相反。

最后定位到那个坏计数,靠的是一个**放在"会停的那段"之外**的探针:它盯的正是"要不要睡过去"这个决定本身。

> **规矩:至少留一个探针在"会停的区域"之外,并且让它报告"我在、我看到什么",而不是靠"没有报错"来推断没事。**

---

## 八、测量工具也搬进了仓库

*2026-07-31 13:40 · `404b16b0`*

跑实验的脚本、出报告的脚本、图,全部放进 `benchmarks/live_agent/`。理由很简单:**它们测的就是这份代码,得跟着一起走版本**,分开放迟早对不上。

---

## 九、有些数字本来是假的

*2026-07-30 23:36 / 23:41 · `d6ba9563` 每段都报输入量 · `38589f1f` 真的去测段间传输*

**场景。** 想知道"说话那一段"每轮拿到多少输入 —— 因为怀疑它是慢的主因。

**原来会怎样。** 两件事让这个问题没法回答:

- 统计输入量的代码被一个条件挡着,**只统计第一段**。说话那一段的输入量**根本没有数**。
- 段与段之间传数据的耗时和字节数,是**写死的 0** —— 不是没测准,是压根没测,填了个 0 上去(原作者自己留了 TODO)。

**现在怎样。** 每一段都报自己的输入量;段间传输的构建和发送都真的计时,按需打开。

**为什么把它放最后说。** 它不改任何行为,用户完全感觉不到。但**前面每一节的结论都依赖它** —— 没有这两个数,"说话那段是不是主因"这种问题只能靠猜。

---

## 十、念稿子的那部分,一直背着它从不看的画面

*2026-07-31 16:45 · `796795ac`*

**场景。** 对话不能无限继续:负责发声的那部分迟早会装满,整个请求就得换掉(见第五节)。所以真正的问题是 —— **到底是什么把它装满的?**

**原来会怎样。** 两样东西在填:你给摄像头看的东西,和模型自己说的话。第一样是意外之处:**负责念稿子的那部分,画面的每一小块都要占它一格。** 一张清晰的画面就是 880 格。而它**根本不看画面** —— 它的活只是知道该念哪些字。这些格子还永远不还。

*举个真实的例子:* 某一轮里有三张画面,念稿子那部分背着 **817 格**。其中真正是文字的大约 24 格。剩下全是它用不上的画面和录音。

**现在怎样。** 画面和录音**直接不给它了**。只给文字:你问了什么,以及它接下来要说什么。同样那一轮,**从 817 格降到 33 格** —— 少了大约 **25 倍**。

**为什么认为这样是安全的。** 一条借来的,一条量出来的:

- 另一个团队用同样思路做的模型(MiniCPM-o 4.5),给它念稿子的那部分**只给文字,一张画面都不给**。说明这个设计本来就不需要。
- 这里实测,两边各五轮:**每一轮都照样出声**,两种设置都是。而且两边的账每次都对得上 —— 这一点最关键,因为一旦对不上,是**不会报错的**,只会悄悄念错。

**还不知道的两件事。**

- **声音听起来有没有变。** 五轮、每边一次对话、**没有人真的去听**。这个测试能查出"它不说话了",查不出"它语气有点平"。而且这个模型当初**是带着画面训练的**,所以细微的退化是有可能的。
- **对话现在能撑多久。** 还没测出来。测的时候新设置下模型碰巧啰嗦了一倍,而**模型自己说的话也占同一块地方** —— 多说的那些把省下的吃掉了一半。得把模型的随机性关掉重跑一次,让两边说出一样的话。

**能退回去。** 一个设置就能恢复原来的行为。**故意留着的** —— 不留,以后就没有基线可比,这件事就只能靠回忆了。

---

## 十一、现在可以用浏览器和它说话了

*2026-07-31 19:00 – 20:30 · `2452f4dd` 把这些工作搬到上游 main 上 · `ccff770e` 浏览器客户端 · `5dfc170d` 一份装得下的配置*

**场景。** 你想真的和它聊一次 —— 自己的摄像头和麦克风,它的声音回来 —— 而不是用测试脚本去驱动它。

**原来会怎样。** 只有测量用的客户端。它们从磁盘上的文件里读帧和音频、按固定节奏发、而且**用文字提问**。出数字很好用,但完全不能回答"和这东西说话是什么感觉"。

**现在怎样。** 一个网页。采集你的摄像头和麦克风、持续推上去、把回答播出来。服务器和网页各一条命令,只转发一个端口,而且 `localhost` 算安全上下文,所以浏览器给摄像头麦克风权限**不需要配任何证书**。

大部分**不是从零写的** —— 上游给另一个模型做了一个浏览器客户端,它的麦克风采集和样式表直接原样用了。真正要重写的是"和**这个**服务器说话"那一段,因为两边协议有三处不同,而且每一处都是**静默失败**:

* 音频**上去**是裸采样,**下来**是每块一个完整的声音**文件**。把这些文件首尾相接,等于每一块都往音频里塞 44 字节的文件头 —— 听起来是爆音,而你会怪模型。
* 画面在这边是**独立的消息**,不是挂在音频上的附件。
* **必须有个东西说"我说完了"**,而在这个模型上,这个东西只能是网页。见下。

**要诚实的那部分。** 这个模型**没有办法自己决定什么时候开口** —— 它的词表里没有这个选项,服务器也从不自己开始回答。所以由网页来定:要么它发现你安静了 0.7 秒,要么你按住一个按钮。**网页上写明了这一点**,因为这个仓库里**另一个**模型是自己做这个决定的,拿两者对比的人有权知道到底是谁在驱动。

**效果。** 先在不用浏览器的情况下验证,这样结果说的是服务器而不是某一台笔记本:五轮,每一轮都出了文字和语音,第一声在 **340–376 毫秒**。这和之前量到的 513 毫秒是同一量级,让人放心,但**不是同一个实验** —— 引擎版本不同,并发槽位也少了。

**还有一个差点漏掉的 bug。** "有没有出声?"这个检查通过了。然后换成更长的回答再问一次,答案没变:**34 个字符的回答和 104 个字符的回答,都只送来 0.22 秒的声音。** 只有第一小段语音到了网页,后面的做出来了但丢了。又跑了一遍、把我们自己最新的那个改动关掉,还是 0.22 秒,**所以那个改动不是原因** —— 丢失发生在这个分支继承来的那条路上。**记录下来,没有绕过去**,而且在修好之前,网页里的音质不值得去听。

---

## 十二、画面现在是"你还在说话时"就被看懂的

*2026-08-01 · `a9e4e455` 功能本身,以及压在它底下的两个 bug*

**场景。** 你举着摄像头说几秒钟话。这几秒里画面一直在进来。

**原来会怎样。** 每一张画面都**先放在一边**,等你说完话,才一起转成模型能读的形式 —— 而这份活正好挡在你的回答前面。也就是说,画面的钱是在**最糟的时刻**付的:问完之后、你正在等的时候。

**现在怎样。** 每张画面**一到就转**,趁你还在说。等你说完,这份活已经干完了,只剩问题本身。

**比省下的毫秒更重要的一点。** 老做法意味着每轮多一张画面就多等一会儿,所以摄像头一直被故意压得很慢。一到就转把这个联系切断了:**画面发得更密,不再需要你多等。**

**这件事的代价,而这才是值得读的部分。** 打开它会**直接把语音引擎打死**,每一次,都在第一句回答上。一共试了**五次**,而前四次**以完全相同的方式失败** —— 这恰恰是最误导人的线索:看起来像"同一个原因还没找对",其实是**两个毛病同时发生**,而第一个会在第二个能被看见之前就把进程杀掉。

> **毛病一。** 模型吐出一个词的时候,系统会先在该放这个词的位置写一个**占位符**,下一步再从一份短命的副本里把真词填回去。那份副本**只会送给"上一步还在忙"的请求**。我们那个"现在就把这张画面看懂"的活会把语音那部分**短暂地放到一边** —— 于是它回来的时候真词已经没了,只剩占位符。语音那部分拿着这个占位符去查一张它不属于的表,进程就死了。
>
> **毛病二。** 看画面那一步交给语音那部分的是一个**形状不对的包裹** —— 内容是"这里有一大段新材料",标签写的是"这里多了一个词"。在毛病一修好之前根本看不见它,因为崩溃总是先到。

**由此得到两个习惯,都是硬碰出来的。**

*不要托人带话,要直接看东西本身。* 我们试了**三次**给那份工作**贴标签**,让后面的环节能认出它,用了三条不同的通道。三次**都读到空的**,而且悄无声息 —— 承载标签的那个字段早被下一份工作覆盖了。最后奏效的做法是改问一个关于眼前这份工作的问题:*这一步是不是吃进了一大堆新材料、然后就停了?* 如果是,那就没有什么要念出来的。这个事实**丢不掉**,因为它根本没有被"带"到任何地方。

*日志里少一行,不算证据。* 有两次,我们写的检查是靠**某条消息不出现**来确认事情,两次都错了 —— 代码根本没跑到。现在这里所有的检查都改成靠**出现**来确认:那条消息必须打出来。

**它花了什么,说实话。** 四次错误的尝试,每次都要完整重启一遍引擎,之后才把做法从"猜一个原因然后验证"换成"一次把整条路读完、让代码自己说"。这个教训可以推广:**当几个完全不同的修法以一模一样的方式失败时,别再找那一个没找到的原因,该怀疑有两个。**

**干净的数字,终于有了(2026-08-01,在第十三节全部修复之后重测)。** 每轮画面翻三倍,**关着要多付 41 毫秒,开着只多付 10 毫秒** —— 对帧率的敏感度约低 **4 倍**;回答时间的波动从 43 毫秒收窄到 8 毫秒,对"可预测性"这个论点来说后者更要紧。之前的 **16 倍**是在"有些回答被记到错的轮次上"时量的,虚高,撤回。这正是"记账修好后必须重测"的意义:效果是真的,但只有最初宣称的四分之一。

---

## 十三、声音发糊、话说一半被切 —— 一个症状,三个毛病

*2026-08-01 · `9624cac1` `3093205d` —— 用耳朵听出来的,由一次五路并行的代码与日志审计钉死*

**场景。** 开着"画面一到就看懂"(第十二节),和它聊上几轮。第一句回答没问题;从第二三句起,声音开始发糊,有时话说到一半就断了,再也没有下文。

**一句话背景。** 每张被保留的画面都会派生一个小任务:*"看一眼这张图,什么都别说。"* 问题在于,"别说"这半句**只有负责看的那部分收到了**;负责**说**的那部分把每个小任务都当成要回答的东西:哼几声没有内容的音频,然后宣布一句**"我说完了"** —— 货真价实的宣布,和真回答结束时是同一种。

**毛病一 —— 这声宣布认错了主人。** "我说完了"从语音引擎传回前台要走大约一秒。如果你的下一个问题恰好在这一秒里问出去,前台就把这声宣布当成**你的回答**说完了 —— 于是半句话就收工。真正的回答稍后到达,被判"当前没有轮次在进行",整段扔掉。有一场会话就这样丢了 **27.5 秒**真实语音;日志里每一个坏掉的轮次都正好压在这种碰撞上,而健康轮次一个都没有。

修法不是更聪明的标志位 —— 在"东西到达那一刻"读标志位,恰恰就是输掉这场竞速的原因。修法是一本**提交台账**:活儿从一扇门进引擎、按同样的顺序干完出来,所以第几声"说完了"就属于第几个活儿,没有例外。看图任务的哼哼和宣布现在被点名丢弃;只有你问题自己的那声宣布才能结束你的轮次。

**毛病二 —— 接缝不再缝了。** 声音是一小块一小块拼出来的,每块都要和上一块的尾巴缝在一起 —— 像贴墙纸要对花纹。缝合算术里有一个**从不清零**的计数器,却被拿去丈量一张**每轮都清空**的清单。结果从每场对话的第二轮起,每句回答的第二块都是**完全不带重叠**硬拼上去的 —— 那道缝就是你听到的"糊"。同一套错误算术碰到很短的一段时,还会算出负的长度,把那段音频整个扔掉。会在正确时刻清零的计数器代码库里本来就有,兄弟模型的代码早就在用。

**毛病三 —— 哼哼没有上限。** 前两个修好才看得见:那些无内容的哼哼是**不设长度限制的**。有一个任务哼了 **50 秒**,而说话那部分一次只干一个活,下一句真回答的*声音*只能排在它后面 —— 文字 2 秒就好了,声音 52 秒才响。现在,凡是到达时没带任何内容的活儿,最多出一个音就被叫停。叫停机制也是本来就有的,只是从来没接到说话这一级上。

**这一轮添进清单的习惯。** 第五节和第十二节的结尾都是"是两个毛病,不是一个"。这是第三次 —— 次数本身就是教训:当一个症状在看起来正确的修复之后仍然存在,下一个假设不该是"修错了",而该是"下面还压着一个"。另一条也值得留下:决定性的证据早就躺在旧日志里 —— 几周历史中每个坏轮次都开着这个功能,每场关着它的会话都干干净净。一场没人需要做、只需要去读的天然实验。

**验证过,不是推断。** 按浏览器的真实节奏对着运行中的引擎重放 —— 静止画面逼出每 2 秒一个看图任务,八个问题专门掐在碰撞窗口里发。八轮全部完好,每声宣布都被正主认领,上限在每个任务上布防又解除,50 秒的尾巴消失。同样的压力在修复之前,几秒钟就打断了第二轮。

---

## 十四、十六个人同时用 —— 等待扛住了,寿命没扛住

*2026-08-01 首测(bf16)· 2026-08-02 换 FP8 引擎后全部重测 · `e91735b3` `cd399ae0` `ba64c261` `73131a2b` —— 15 格矩阵:1/2/4/8/16 个用户 × 三种画面*

**场景。** 前面十三节都是一个人用。这一节问云上的问题:N 个人同时压在同一张卡上,等待会恶化多少、多快?每个模拟用户都是一条真连接,走浏览器同一扇门,每秒发 2 帧真实视频,进行 30 轮闭环对话(问、听完整个回答、想几秒、再问)。画面用七月研究的同三套:静止屏幕、说话人、手持行走。每一格都在刚启动的干净引擎上开跑;回复长度被提示词钉死(15 格全部是中位 27–29 字符),所以等待的差异只能来自人多。

**量出来的等待**(FP8 引擎,全部 15 格满轮跑完、无一死亡。三个数依次是 P50 / P95 / P99,毫秒。P99 剔除了每格开机后的第一轮 —— FP8 冷启动首轮要 2–4.5 秒,是开机付一次的钱;另外每格样本 60–480 个,P99 应读作"见过的最坏几轮",不是严格分位数):

| 用户数 | 静止屏幕 | 说话人 | 手持行走 |
|---|---|---|---|
| 1 | 380 / 454 / 499 | 385 / 473 / 496 | 399 / 517 / 551 |
| 2 | 439 / 695 / 695 | 421 / 743 / 743 | 453 / 902 / 902 |
| 4 | 575 / 794 / 813 | 614 / 797 / 832 | 673 / 977 / 1,048 |
| 8 | 693 / 965 / 1,074 | 700 / 932 / 985 | 839 / 1,317 / 1,406 |
| 16 | 783 / 1,250 / 1,416 | 798 / 1,069 / 1,121 | **1,405 / 4,673 / 5,368** |

**十六个人的慢,到底慢在哪。** 表里唯一难看的格子(手持 × 16)不是抖动,是匀速恶化:第 2 轮 0.9 秒,第 30 轮 3.3 秒,最慢的 5% 轮次全部落在第 22 轮之后。把每轮切成两段、只看绝对毫秒数(16 人,早期 → 后期,P50/P95):

| 段 | 早期(2–8 轮) | 后期(18–25 轮) |
|---|---|---|
| 想字(提问 → 第一个字) | 202 / 541 | **1,151 / 2,404** |
| 出声(第一个字 → 第一声) | 686 / 954 | 1,297 / 2,216 |

再把最慢 5% 轮次发生的那几秒的硬件采样单独拎出来:**SM(算力单元)93% 打满,显存带宽只用 18%**;按进程拆,思考引擎在慢轮里独占 64% 的算力(中位轮 50%),说话引擎反而从 30% 掉到 21% —— 它不是变慢了,是**抢不到卡**。所以两段一起变慢,根子是同一个:思考引擎把算力吃光了。

**为什么图片多了,瓶颈是算力而不是显存带宽。** 引擎里只有两种活:

- **prefill(消化输入)**:一帧画面变成 220 个 token,**一批**进引擎 —— 全部历史从显存搬出来**一次**,220 个 token **每个**都拿它做一遍乘法。搬运 ×1,计算 ×220;
- **decode(生成回答)**:一次只产 1 个字 —— 同样搬全部历史,却只做 1 份计算。搬运 ×1,计算 ×1。

这张卡的体质是:每搬 1 字节,算力配得起约 110 次乘法。decode 每字节只用 1–2 次(带宽先堵,这是"大模型推理是带宽瓶颈"这句老话的来源);prefill 每字节要 ~350 次(算力先堵)。给一个具体的账:历史 4.5 万 token 时,一帧的 prefill 要**搬 3 毫秒、算 40 毫秒** —— 计算是搬运的 13 倍,搬运车间干完就闲着。而我们的负载里,每轮回答只有 ~30 个 token(decode 型),涌进来的帧却有 ~1,500 个 token(prefill 型)—— **96% 的活是 prefill 型**,整张卡表现出 prefill 的体质:SM 打爆、带宽闲逛。总需求一个乘积说尽:**人数 × 每秒帧数 × 每帧的历史厚度**。前两个因子固定,第三个逐轮上涨 —— 16 人高动态在第 ~13 轮越过产能线,之后全是排队。

**反例恰好证明了这条规律:没有图片的 duplex,先崩的是带宽。** 我们在 Moshi(纯语音全双工模型)上做过多用户测试:它没有任何"成块进上下文"的输入 —— 声音按节拍逐步进,每 80 毫秒一步、每步 1 个时间片,负载 100% 是 decode 型,每字节只配 1–2 次乘法,而且是稠密模型、每步全量权重都要搬 —— 多用户下**显存带宽先出问题**,和这里正好相反。两套系统、两面相反的墙、同一个判据(每字节配几次计算)统一解释。一句话:**实时代理一旦长出眼睛,瓶颈就从显存带宽迁移到算力**;药方也随之不同 —— 带宽墙的药是"少搬字节"(量化、加大批次),算力墙的药是"少做乘法"(历史封顶、按乘积准入),互相不通用。

**墙(bf16 时代的历史,FP8 重测中已不再出现)。** 每场对话的上下文只涨不缩,而思考那级的记忆池是启动时定死的,人多池子不变大。于是池子会在**可以算出来的轮数**耗尽:池子容量 ÷(人数 × 每轮增量)。增量由一个数决定 —— 过滤器每秒保留几张画面 —— 这个公式命中了矩阵里的每一次死亡,包括一次开跑前写下的预测(16 人 × 手持:预测第 4 轮上下,实测 3–5 轮)。轻画面保留 25% 的帧,8 个人第 ~18 轮死;手持保留 56%,**4 个人也是第 ~18 轮死** —— "四个人是安全的"说的是摄像头,不是卡。

**FP8 是怎么拆掉这堵墙的。** 把思考模型的权重和 KV 都转成 FP8(装载时由 vLLM 现转**官方**权重;说话和波形两级保持 bf16),记忆池从 10.7 万涨到 **73.2 万 token**,上面整张表就是换血后的全量重测:15 格无一死亡,文字与 bf16 逐字一致,延迟持平。路上三个教训:社区的预量化检查点能加载但说胡话(弃);0.74 的显存配额是按 bf16 调的,FP8 算子的工作区把余量吃穿导致 8 人 OOM —— 降到 0.70,用 8 万 token 的池子换 4 GB 瞬时工作内存,这才是并发数的真约束;人耳听感已过(正常普通话对话确认),但出现过**一次**会话级怪事 —— 某个会话从第一句起整场说粤语,新开会话即恢复,机制指向"第一句语音的语言判断落在边界上、随后被会话历史锁定",复发时保留现场再查。

**三次崩溃的原因。**

1. **8 人集体死锁 —— 设计问题:被挂起的会话,丢失的信号没有补发机制。** 池满时调度器把会话请求挂起腾地方,这是正常操作;但"这句话说完了"的信号只投递一次,挂起期间错过就永远没有。回合因此永远关不上,之后的提问全被"上一轮还在进行"拒绝。健康检查只测 HTTP、不测音频,死锁全程显示"正常"。**延迟问题变成了寿命问题。**
2. **16 人引擎崩溃 —— 实现问题:显存配额不管运行时峰值。** `gpu_memory_utilization` 只决定记忆池划多大;16 路同时预填画面的瞬时激活内存没有任何参数约束,峰值一超过剩余显存,stage-0 直接 CUDA OOM,进程死亡。
3. **2 人同批崩溃 —— 实现问题:多请求拼批代码从未运行过**(已修,`e91735b3`)。stage-2 按"整批总长度能否被 16 整除"选处理路径;批里混入一个 1-token 短段时总长不整除,代码把 N 个请求拍成一行、又按 N 个去切 —— 下标越界,引擎崩溃。修复:每个请求各自补齐长度。同段代码两个连带问题:有请求缺"接缝"字段时**所有人**的接缝参数被清零(已修,同一提交);大小不同的音频块同批时接缝错 ~23 毫秒(未修,基准期间不改音频输出)。

---

## 十五、上下文压缩 —— 换本子这件事,用户感觉不到了

*2026-08-02,提交 `0b37129c` / `22ce6ea6`。*

**要解决的问题。** 聊得越久,模型的记忆本越厚。厚了有两个坏处:多人同时用时,每一轮
都比上一轮慢(第十四节那条爬坡);单人聊到 65,536 个 token 会撞模型的记忆上限,撞上
之后会发生什么,从来没人验证过。老办法是"写满了当场换新本子":用户提问的那一刻删掉
旧对话、重建新对话,那一轮要多等 3.70 倍。

**现在的做法:后台先备好新本子,到点只切一下。** 四步:

1. **数着。** 系统里有两个模型,各有一个会写满的记忆本 —— thinker(想:看画面、写回答)
   的本子主要被**画面**写满;talker(说:把回答变成语音)的本子被**它自己说过的话**写满。
   两个本子写满的速度没有关系(画面多话少,前者先满;画面静话多,后者先满),所以各盯
   各的,谁过警戒线都启动准备。
2. **备新本。** 趁用户不说话的空隙,悄悄开一个新对话,把"最近几轮的问答文字"提前塞进去
   读完,放着待命。旧对话照常服务,用户毫无感觉。备的过程中 talker 会对着这段文字咕哝一小
   段废话语音,系统**等它咕哝完、把废话扔掉,才算备好** —— 这一条是拿实测教训换来的,见下。
3. **换。** 用户下次提问的瞬间,把问题发给新对话。新对话早就读完准备好了,所以这一轮和
   平常一样快。备新本期间又聊过的话,文字也随这一轮补进去,一句不丢。
4. **删。** 旧对话等这一轮说完话再删,腾出显存。特意等说完:删除动作不和正在进行的对话
   抢跑(老办法里睡 1 秒防的就是这个,现在用先后顺序防)。

换完之后,新对话里有:最近几轮的**文字** + **当下的画面**。旧画面没有了 —— 这是每次
压缩付的代价。实测这个代价可控:文字扛住了 3/3 的内容回忆("我叫什么""我喜欢什么颜色"
换本后全答对),答不上的只有旧画面里的视觉细节。

**什么时候触发。** 两个本子各有两条线,默认配置的具体数字:

| 本子 | 准备线(过线就开始备新本) | 强制线(新本一直备不出来才走到:当场换,慢 3.70 倍) |
|---|---|---|
| thinker 的(画面为主) | 49,152(= 模型上限 65,536 的 75%) | 60,293(= 上限的 92%,保证永远撞不到 65,536) |
| talker 的(语音为主) | 38,250(= 45,000 的 85%) | 45,000(这面墙撞上引擎会死,不能赌,到线立刻换) |

这条线**不分单人多人,统一按模型上限的 75%**(潇的定案):压缩只负责一件事 —— 寿命,
让会话永远撞不到 65,536;多人场景下历史变厚带来的延迟,是调度要解决的另一个问题,不让
压缩兼职。统一政策带来一条硬容量规矩:**并发人数 ≤ 记忆池 ÷ 触发线**(这张卡:73.2 万 ÷
49,152 ≈ 14,留余量取 13)—— 超过它,池子会在触发线之前耗尽,见下面 16 人的实测死亡。

**实验配置(下面的数字在什么条件下成立)。** 一张 96 GB 的卡(RTX PRO 6000
Blackwell),三个模型阶段同卡:thinker 用 FP8 权重 + FP8 记忆(KV),talker 和波形级保持
bf16。显存怎么分:thinker 拿 70%(权重 31.4 GB + 记忆池 33.5 GB),talker 10%,波形 8%,
剩下是运行时缓冲的余量。**记忆池共 731,904 个 token,全场会话共享,启动时定死。**
并发名额 20;每帧缩到 640×352(220 个 token);内容是最费的手持高动态;每 0.5 秒送一帧,
过滤后每轮实际进 4–5 帧;问题短句、回答一两句。

**为什么这张卡最多撑 13 个用户。** 统一 75% 政策下,每个会话的记忆最多长到 49,152 个
token 才换本 —— 也就是说每人要在池子里占住一块最大 49,152 的地盘:

| 并发 | 峰值合计占池 | 结果 |
|---|---|---|
| 13 人 | 639k = **87%** | 实测通过(剩下的 13% 给换本时新旧本短暂并存、以及日常周转) |
| 14–15 人 | 94–101% | 理论临界,未实测 |
| 16 人 | 786k = **107%** | 实测死亡 —— 池子在人均 ~45k 时(合计 98%)就见底了,谁也没活到换本线 |

要撑更多人,三条路:换更大记忆池的卡、把触发线调低(等于回到延迟旋钮的用法,非当前政策)、
或者做真正释放显存的细粒度删除(第二把刀,架子上)。

**测出来什么样(三个规模,同一条 75% 线)。**

**单人 × 48 轮:** 全程 p50 371 / p95 409 毫秒,一条直线。第 34 轮 context 到 49,217,
自动触发;第 35 轮无感换本(409 毫秒,邻轮 373/365)。**第 44 轮起,会话活在了"原本的
坟墓"里** —— 不压缩的话,累计输入在第 ~43 轮就顶到 65,536,这是第一个活过那个点的会话。
换本后的记忆也验过:另一场把线压到 2,500、强制换两次本的冒烟里,"我叫什么""我喜欢什么
颜色"这类跨界问题 3/3 全对。

**13 人 × 48 轮(容量边界内,13 × 49,152 = 池子的 87%):** 624/624 零超时,13 次换本
全部无感,0 次退回老办法。曲线是教科书式的锯齿:

| 轮次 | 每轮 p50(毫秒) |
|---|---|
| 第 10 轮 | 821 |
| 第 26 轮(锯齿顶) | 2,689 |
| 第 30 轮(换本之后) | **815** |
| 第 38–48 轮 | 900–970,平 |

锯齿顶的 2.7 秒是 75% 政策在 13 人下的代价 —— 压缩管的是寿命,不管这个;把这个尖压下去
是调度(急活插队)的活,另一把刀。

**16 人 × 48 轮(容量边界外,16 × 49,152 = 池子的 107%):** **全员死亡,且死在触发线
之前一步。** 人均 ~45k 时(合计 = 池子的 98%)记忆池耗尽,所有会话卡在"分配不到显存块"
的等待里,回合信号永远不来,每人 3 次超时后被判弃;整场 0 次换本 —— 没有一个会话活到
49,152。死亡全程被第七节的自检探针实时转储,一条日志不缺。这场死亡就是上面那条容量
规矩的实测证明。

**本节所有场景的延迟汇总(TTFA,毫秒;都在统一 75% 线下,除最后一行)。**

| 场景 | p50 | p95 | **p99** | 最大 | 超 2 秒 | 形状 |
|---|---|---|---|---|---|---|
| 1 人 × 48 轮 | 371 | 409 | **437** | 437 | 0% | 平线 |
| 13 人 × 48 轮 | 968 | 2,915 | **3,537** | 3,885 | 14.6% | 锯齿(顶在第 22–29 轮) |
| 16 人 × 48 轮(超容量) | 1,161 | 3,765 | **5,347** | 5,915 | 26.0% | 死亡前存活轮的数字;第 24 轮起陆续回合丢失 |
| 对照:16 人不压缩(第十四节基线) | 1,378 | 4,477 | **5,368** | 10,194 | 38.4% | 只涨不回的爬坡 |
| 旁注:16 人 @ 16,000 手动线 | 871 | 1,320 | **1,860** | 6,498 | 1.0% | 低锯齿(非当前政策) |

*(旁注:压缩机制本身有能力兼任延迟管理 —— 16 人把线手动调到 16,000 时曾测得 p95
1,320 毫秒、40 轮全平、零死亡。按当前政策不这么用,记录在此只为标明机制的能力边界。)*

**踩过的三个坑。**

1. **备新本时不能让模型"只读不说"。** 那样 talker 收到的第一页是空的;等换本后第一轮
   真内容到达,页码按全文算、数据只有增量,对不上号,stage-1 引擎当场死。修:备本时让它
   读完后说两个字再停 —— talker 收到的第一页就和任何普通对话的第一页一模一样。
2. **"备好了"必须等废话语音放完再算数。** 同一个对话里的活是排队干的:切早了,用户的
   真话排在废话后面 —— 实测 640 轮里有 8 轮"文字 0.1 秒就到、声音等 6 到 26 秒",全部
   出在这。改成"废话放完才算备好"后,零再犯;等待全部发生在后台,旧对话照常服务。
3. **影子也占引擎的并发名额。** 名额要留余量(16 人配 20),抢不到名额的影子只是晚一轮
   换本,不是错误;但余量本身费显存 —— 配到 34 直接把波形那级挤到启动就崩("显存配额
   只管记忆池、不管运行时缓冲"这一课第三次应验)。

**开关**(都在 `session.config`,`context_compression_trigger_tokens` = 准备线,默认
自动取模型上限的 75%,传 0 关闭;`context_compression_target_tokens` = 换本时最多带走
多少文字,默认 4096;`context_compression_warmup_timeout_s` = 备新本最多等多久,默认
30 秒,超时放弃、退回老办法)。部署两条硬规矩:并发名额 `max_num_seqs` ≥ 会话数 + 影子余量;并发人数 ≤ 记忆池 ÷ 触发线。

---

## 现在的状态

**能用的**

- 单用户、开着摄像头和麦克风、连续长时间对话
- 每轮只处理新画面;画面自动缩小;取帧节奏有上下界
- 对话**可以无限继续**(自动换请求,用户察觉不到)
- 撞上限前有预警,上限距离每轮可见
- 三类静默故障会自己报出来
- 念稿子那部分不再背着画面 —— **每轮要拿的东西少了 25 倍**
- **一个浏览器客户端**:自己的摄像头麦克风进去,它的声音出来,只转发一个端口

**验证过的**

- 40 轮 + 中途换 2 次:没崩、没卡。同设置**不换的话第 27 轮就结束**
- 50 轮不换请求:50 轮全部完成,收尾干净
- 长会话的记忆能力没有变差(**在不换请求的前提下**)
- 把画面从念稿子那部分撤掉后,**每一轮**都照样出声(两边各 5 轮)
- 新引擎上的浏览器链路端到端:**五轮全部出声,第一声 340–376 毫秒** —— 在不用浏览器的情况下验证的,所以说的是服务器

**明确还没做 / 还没测的**

- **换请求之后的记忆没测过。** 按原理,文字带过去了、画面没带过去,所以**预期**是"记得说过什么、忘掉更早看到什么" —— 但没测就是没测,不能当结论。
- **多用户测过了(第十四节),但还活不下来。** 等待随人数扩展得很好;可共享记忆池一满,会话就在可预测的轮数**死掉**。能改变这件事的两个修复 —— 被挂起也能活、按上下文增速而非人头放人 —— 只在纸面上,还没建。
- 有一个"自愈"补丁能把卡住的任务捞回来,但**为什么会卡成那样,原因还不知道**。
- 那个坏掉的计数,我们只是**绕开**了,**没有去上游修**。
- 画面从念稿子那部分撤掉之后,**没有人听过音频**,而且**对话现在能撑多久也没测**(见第十节)。
- **浏览器里每轮的音频被截断到 0.22 秒**(见第十一节)。链路是通的,声音还不行。已经用 A/B 排除是我们的改动,但原因还没找到。
- **新引擎上的数字不等于旧数字。** 513 毫秒和那次 40 轮滚动都是在旧引擎、另一份配置上量的;跨两边比较之前必须重跑。

---

## 附录:配置项与代码位置

给需要找代码的人。每节标题下面的斜体行就是该改动的日期和提交号。
代码都在本仓库的 `live-agent` 分支,共 26 个提交、8 个文件、约 1,700 行。

第十节是唯一**不在** `session.config` 里的开关 —— 它改的是"一个模型阶段交给另一个阶段什么",
比"单次对话的配置"能触及的层次更低,所以做成了环境变量:`VLLM_OMNI_TALKER_TEXT_ONLY`,
**默认开**,`=0` 恢复原来的行为。

其余的配置项都在 `session.config` 里,默认值:

`max_frame_width` / `max_frame_height` = 不缩放 · `frame_jpeg_quality` = 90 ·
`frame_filter_min_gap` / `max_gap` = 不限制 · `session_scoped_request` = 关 ·
`session_talker_token_budget` = 无 · `session_roll_at_talker_tokens` = 关 ·
`session_roll_history_turns` = 8 轮 · `session_roll_settle_s` = 1 秒 ·
`context_compression_trigger_tokens` = 自动(模型上限的 75%;0 = 关)·
`context_compression_target_tokens` = 4096 · `context_compression_warmup_timeout_s` = 30 秒
