# A 在默认的 vllm-omni + Qwen3-Omni 之上多了什么

这份文档只写**差异**。原版已经有的东西这里不写;每一条都是这个分支加的,写清楚原版是什么行为、
A 改成什么、量出来什么。之前那份按时间顺序的建设记录还在 git 里,在 `f9406958:workflow.zh.md`。

**起点。** 原版 vllm-omni 把 Qwen3-Omni 拆成三段跑(thinker → talker → code2wav),而且已经有一个
流式视频的 WebSocket 入口,带 EVS 相似帧过滤。它做得好的是**一个请求**:提示词进来,token 出来,
请求结束。

**A 必须是什么样。** 几十个人,每人开着摄像头和麦克风,每人聊几十轮,而且**只要还连着,每人每秒
就欠 12.5 个语音编码帧**。这不是"一个请求的放大版"。它需要三件原版没有的东西:

1. **会话语义** —— 一次对话是一个长期存在的东西,不是一串请求;
2. **多租户** —— 那些墙(KV 池子、每轮的 token 预算)是共享的,所以每一道门槛都必须除以租户数;
3. **每帧、每 token 的固定开销要低到 N 份能实时装下** —— 而最后卡住的是调度和传输,不是算力。

---

## A 怎么算合格

两个数,由 `benchmarks/thinker_talker/analyze.py` 从客户端自己记的到达时间算出来,每个会话的前两轮
当预热扔掉:

- **错过截止时间的比例** —— 所有音频块里,有多少块到的时候前面已经播完了。播放时间轴在每次错过之后
  **重新对齐**,所以一次晚到不会被它后面每一块重复算一次。
- **每轮卡顿毫秒数** —— 听的人实际听到的静音,按轮汇总。

**判定:错过 < 1% 且 卡顿 < 50 毫秒/轮。**提前到的块不算问题:允许模型跑在播放前面,客户端会缓冲;
只有把缓冲追平之后的空档才算。第一声的延迟照样报,但**不参与判定** —— "开口晚"和"中间断"是两种
不同的故障、原因也不同,把它们平均成一个数,正是真正的原因被藏了好几周的原因。

---

## 全部差异一览

| # | 项 | 原版 | A | 量出来 |
|---|---|---|---|---|
| **会话语义** ||||
| 1 | 一次对话的形状 | 每轮一个新请求,历史重新预填充 | 一个会话一个可续的请求,每轮只追加新增的部分 | 留着历史比丢掉更**便宜** |
| 2 | 帧什么时候变成 token | 提问的时候 | 一到就变,趁用户还在说话 | 对第一声延迟影响最大的单项 |
| 3 | 麦克风音频什么时候变成 token | 提问的时候 | 一到就变,按 1 秒一段,最后 1 秒留着接尾巴 | 编码器是 1 秒卷积 / 8 秒注意力,1 秒 = 延迟最优 |
| 4 | 对话能聊多久 | 撞上下文墙就结束 | 在用户底下自动换请求并压缩,察觉不到 | 40+ 轮;原理上无上限 |
| 5 | 哪一帧回答"现在" | 过滤器留下什么算什么 | 最新那一帧跟着提问一起进去 | 新鲜度买的是位置,不是帧率 |
| 6 | 某一段悄悄停了 | 日志里什么都没有 | 三类静默卡死会自己报出来 | 观察到的故障就是"出了文字,然后永远没有声音" |
| **多租户** ||||
| 7 | 过载 | 排队 | **在门口拒掉** | 让一个周期性的流去排队,本身已经违约 |
| 8 | 共享 KV 池子 | 只有按请求的上限 | 准入时把每个池子除以在线租户数(thinker 和 talker 两道) | 64 会话、talker 池 116k 时每个请求都被抢占,p99 57 秒 |
| 9 | 所有人同时压缩 | 无(没有压缩) | 确定性 16 档相位阶梯 + 2 秒全局日程表 | 浪潮里的秒迟到 10.7%,无事件的秒 2.5%,补的就是这个差 |
| **每帧 / 每 token 的开销** ||||
| 10 | 帧尺寸 | 原样(1280×720 = 880 token) | 一到就缩到 640×352 = 220 token | 每帧提示词少 4 倍 |
| 11 | 去重的上下界 | 有相似帧过滤,但间隔无界 | `max_gap` 强制放行一帧,`min_gap` 压住放行速率 | 分别管住"看不见"(静止画面)和"太贵"(画面乱动) |
| 12 | talker 收到什么 | thinker 的完整载荷,画面也在里面 | 只有文字 | 每轮要拿的东西少 **25 倍** |
| 13 | 解码步的载荷 | 词嵌入 + 隐藏状态行 + 完整输出 id 列表 | 只有 talker 会读的那个字段 | 17458 → **8922 字节**,而且不再随轮长平方增长 |
| 14 | JPEG 解码 | 在事件循环上 | 8 个预热好的子进程 | 循环不再和逐帧解码抢时间 |
| 15 | 块的传输 | **每个块**都创建/加锁/写入/删除一遍 | 每个(会话,通道)一段常驻共享内存,两个槽 | 序列锁 + 确认格;超大或未确认退回老路,块不会丢 |
| 16 | 发一个块 | 交给发送线程 | 调度线程上 T+0 当场写进通道 | 每个载荷少一跳 |
| 17 | 声码器卷积 | 全窗 | 带窗 | 采样**逐位相同**,工作量少一半左右 |
| **引擎节奏与部署** ||||
| 18 | 靠 chunk 喂的那一段 | **停车**,下一轮再放回来 | 在调度线程上当场取件 | 56 会话:错过 **20.1% → 0.9%**,卡顿 35 毫秒,时间线变平 |
| 19 | code2wav 放哪 | 本来就是自己一个进程 | 还是自己一个进程 —— 共进程是我们加的,量过之后关掉了 | 共进程时 GPU1 只有 **50%**(独立进程 96%);64 用户 5.88%/307 毫秒 vs 0.41%/17 毫秒 |
| 20 | talker 的 CPU/GPU 重叠 | stage 0/1 都没设 | 明确写:stage 1 **开**,stage 0 关 | stage-1 每轮时间砍一半;只有和 #18 一起才安全 |
| 21 | 精度 | bf16(什么都没设) | thinker 和 talker 都用 FP8 权重 + FP8 KV | thinker 59.4 → 31.7 GiB,每 token 的 KV 减半,池子约 6 倍 |
| 22 | CUDA graph | 三段本来就都开着 | 一样 —— 我们早期单卡配置用过 eager,后来退回来了 | eager 的 talker 在 32 用户时只有 29% 时间有核在跑 |
| 23 | 声码器每次算多少 | 每次 25 个编码帧(2 秒音频) | **4** 帧(320 毫秒) | 播放流畅度的颗粒度就是这个数 |
| 24 | 尺寸(`qwen3_omni_moe.yaml` → `deploy_2gpu.yaml`) | 64 槽 · 0.9/0.6/0.1 · 每轮 32768/32768/65536 · 不开前缀缓存 | **80** 槽 · 0.90/**0.30**/0.10 · **16384/8192**/65536 · thinker **开**前缀缓存 | stage-1 池子约 100 万 token,每会话约 5 万 |
| 25 | 段和卡的对应 | 原版示例就是 thinker 在 GPU0、talker+声码器在 GPU1 | 不变 | 这台机器没有 NVLink,所以跨卡的只有文字载荷 |
| **每一步的数据搬运** ||||
| 26 | 每步的载荷行 | 非锁页 `.to(device)`,每会话每步一次 | 先进锁页缓冲,拷贝异步入队 | 4 KB 花 147 μs → 176 会话:卡顿 27.2% → 2.03%,容量 ~168 → ~180 |
| 27 | 码本预测器的 15 个 AR 步 | 每步都重新前向整个 17 位置窗口 | 固定大小 KV cache:每步只算一个位置,对 17 个槽做注意力 | 每轮 35.4 → 8.6 ms;184 会话:卡顿 3.6-6.8% → 0.12%,容量 ~180 → ~200 |

---

## 会话语义

### 1. 一个会话一个可续的请求

**原版。** 每问一句都是一个新请求,带着整段对话,所以聊到第 20 轮要把前 19 轮的画面和文字重新预填充
一遍才能回答。

**A。** `session_scoped_request` 把整段对话作为**一个**引擎请求提交,每轮作为增量的流式更新喂进去。
一轮只预填充自己新增的帧和这句话,前面的东西一直留在显存的 KV 里。stage-0 → stage-1 的通道也只传
增量,不再每轮把整个提示词的嵌入重抄一遍。

**效果。** 语音那一段不再因为"聊得久"而变慢。原来最慢的情况 —— 几万 token 的历史 —— 现在比"把历史
全丢掉"**更便宜**。

### 2 和 3. 帧和麦克风音频一到就变成 token

**原版。** 上一次回答之后用户发的所有东西,都在提问的时候才预填充,而这段时间正压在"第一声"的关键
路径上。

**A。** `prefill_frames_on_arrival` 和 `prefill_audio_on_arrival` 把每一张留下的帧、每一秒说的话,
作为**只预填充**的 chunk 追加到那个活着的请求上,趁用户还在说。音频用 `audio_prefill_chunk_s = 1`,
并且留 `audio_prefill_reserve_s = 1` 不填,这样这一轮自己的尾巴能正确接上。Qwen3-Omni 的音频编码器
是 1 秒卷积加 8 秒注意力:按 8 秒切和一次性编码逐位相同,按 1 秒切延迟最低,这个部署选延迟。

**限制要说清楚。** 三种情况下这次追加会被拒绝:会话正在答话、这一轮的提问已经被认领、输入队列非空。
原因是顺序:引擎返回的音频段本身不带身份,唯一能知道某一段属于哪个提交进去的 chunk 的办法,就是返回
顺序和提交顺序一致。应用维护一个先进先出的标签队列,每个音频段结束弹一个;如果在一轮答话进行中插进
一次追加,那**下一个**段边界就会被错配 —— 这一轮的音频被当成追加的,客户端会被告知这一轮结束了,而
其实没结束。所以**答话过程中新到的帧不会被预填充**,它们要等,下一次提问替它们付账。

### 4. 对话不会结束

**原版。** 上下文一直长到模型上限,然后这个会话就完了。按每 token 48 KB 算(48 层 × 2 × 4 个 KV 头
× 128 维),13k 的上下文大约是每会话 624 MB 的 KV,所以人多的时候这堵墙比单人时来得早得多。

**A。** 两个机制,一个目标 —— 用户察觉不到:

- **换请求**(`session_roll_at_talker_tokens = 45000`):关掉旧的引擎请求,用最近的文字记录做种子开
  一个新的。
- **上下文压缩**(`context_compression_trigger_tokens`,目标 = 触发线的一半):把旧上下文总结成一个
  滚动窗口,用它预热一个**影子**请求,然后把活着的会话切换过去。`context_compression_carry_frames`
  决定要不要把最近的帧一起带走;A 只带文字,种子便宜,代价是每次切换都忘掉摄像头看过的东西。

### 5. 最新那一帧负责回答

`fresh_frame_on_query` 会把最新到达的那一帧(不看相似度过滤的结果)接在提问增量的末尾。回答"贴不贴
当下",买的是这一帧在提示词里的**位置**,不是更高的帧率:帧率提高,token 线性变贵,而最新那一帧照样
被埋在中间。

### 6. 静默本身是个 bug 报告

一段**崩了**会留下 traceback;一段**悄悄停了**什么都不留,而这正是观察到的故障 —— 客户端收到一轮的
文字,然后永远没有声音。A 现在会在"还有请求被跟踪、但这一段不再前进"时检测并把状态打出来,会记录一个
流式段的停车/继续/唤醒三个事件(`VLLM_OMNI_LOG_SEG_CYCLES`),心跳报的是"有活在跑"而不是"没有报错"。

---

## 多租户

### 7 和 8. 准入直接拒绝,每道门槛都除以租户数

**原版。** 请求排队。按请求算的长度上限,看不见这个池子还有另外 63 个会话在用。

**A。** 一个会话是一个持续的承诺,所以过载在门口拒掉,而不是让已经连着的人一起变差:

- 一个硬的会话上限:`VLLM_OMNI_ADMIT_MAX_SESSIONS`;
- **talker** 那道:`0.75 × stage1 池子 / 在线数 < 2048` token 就拒;
- **thinker** 那道:`0.75 × stage0 池子 / 在线数 < 4096` token 就拒。

为什么要有这两道:64 个会话、talker 池子只有 116,384 token 的时候,每个 talker 请求都被抢占,重算风暴
把 p99 顶到 57 秒,而带宽和算力都是闲的。墙是共享的,门槛就必须做除法。

同样的算法决定**自动的压缩触发线**:`0.75 × stage0 池子 / 会话上限`。原版的锚点(模型自己上下文上限
的 75%)对多租户是错的:64 个会话按"65,536 的 75%"算要 310 万 token,而池子只有 113 万。

### 9. 不要所有人在同一刻压缩

**原版。** 没有这回事 —— 没有压缩可同步。

**A。** 同时开始的会话同时装满、同时越过触发线,N 个影子预热挤在同样的几秒里预填充。那个窗口里量到:
含有一次预热启动的秒迟到率 **10.7%**,不含任何事件的秒 **2.5%**。这个悬崖是可复现的:t+120 秒起,
150 秒到峰值,210 秒归零,和第一次压缩事件对齐。两个修复,都是冲着"同时"去的,不是冲着工作量:

- **确定性相位阶梯**:第 *i* 个进来的会话拿触发线的 `0.55 + 0.45·(i mod 16)/16`,只往下调。原来的
  ±20% 随机抖动量过之后不够 —— 增长速率太一致,把 108 次首压重新挤回一个约 90 秒的窗口里。
- **全局日程表**:`VLLM_OMNI_COMPRESS_MIN_INTERVAL_S = 2`,把启动速率压在 0.5/秒,而稳态需要约
  0.25/秒。

这里还有第三个改动,算稳定性不算延迟:浪潮点火的那一刻,一个空载荷竞态会直接打死 talker 的引擎核心。
现在有护栏了(按 talker 的文字宽度构造零行)。

---

## 每帧、每 token 的开销

### 10 和 11. 帧:更小,而且上下界都管住

一帧会变成 `(宽/32)·(高/32)` 个 token,所以两条边各减半,提示词开销就少 4 倍。A 在帧到达时缩到
**640×352**(220 个 token,而 1280×720 是 880 个),而且只缩不放。相似帧过滤用的是上游那个,但加了
两个原版没有的界:`frame_filter_max_gap` 在连续丢掉 N 帧之后强制放行一帧 —— 过滤器的判据是 64×64
缩略图上的整帧均方误差,只有一小块在变的时候它几乎不动,所以共享屏幕可以好几分钟一帧都不留 ——
`frame_filter_min_gap` 则在画面乱动时压住放行速率。`max_frames = 8` 管住缓冲,满了先扔最旧的,对直播
流来说该丢的就是它。

### 12. talker 不再背着它从不看的画面

`VLLM_OMNI_TALKER_TEXT_ONLY`(默认开)把 thinker → talker 载荷里视觉那部分去掉。talker 的活是
"文字 → 语音编码 token",它从来不读画面。每轮要拿的东西少 **25 倍**。这是 A 里唯一不在
`session.config` 里的机制,因为它改的是"一个模型阶段交给另一个阶段什么",比"单次对话的配置"能触及的
层次更低。

### 13. 解码载荷只发被读的那部分

普通的解码步里,talker 只读 `embed.decode`。原版还会发隐藏状态那一行(固定 4 KB,占载荷一半)和完整的
输出 id 列表(随这一轮变长,使每轮传输字节数平方增长),而这两样在接收侧**根本没人读**。量到
**17458 → 8922 字节**,而且不再随轮长增长。`VLLM_OMNI_T2T_LEAN_DECODE=0` 恢复胖载荷做 A/B。

### 14. 媒体解码搬出事件循环

JPEG 解码和缩放搬到一个预热好的 8 子进程池(`media_pipeline.py`、`media_worker.py`),循环只用子进程
返回的字节做一次内存拷贝重建图像。事件循环是给每个客户端打时间戳和投递音频的那条线程,所以它是唯一
一条绝对不能忙的线程。

### 15 和 16. 块的传输:用信箱,不用文件系统那套动作

原版**每个块**都要创建、加锁、写入、删除一段 POSIX 共享内存。A 给每个(会话,通道)留一段常驻内存,
里面两个 256 KB 的槽按块号奇偶轮换,一个序列锁让"读到一半被改"可以被检测出来,读方维护的确认格给
写方做流控(`VLLM_OMNI_MAILBOX`)。超大或者对方还没确认就退回按块走的老路,所以块不会丢。
`VLLM_OMNI_INLINE_SEND` 再把出去的载荷在调度线程上 T+0 当场写进通道,不交给发送线程。

### 17. 带窗声码器

`VLLM_OMNI_STREAM_VOCODER` 让 code2wav 的卷积在一个窗口上算,而不是整段上下文。发出去的采样**逐位
相同**,工作量少一半左右。

---

## 引擎节奏与部署

### 18. 消费方不再每两轮才走一步

**决定容量的就是这一条。** 原版靠 chunk 喂的那一段是这么干的:调度器发现下一个载荷还没到,就把这个
请求从 `running` 里**拿出去**,登记一个异步加载,等下一轮再放回来。这个请求于是**每两轮调度才走一
步** —— 一个会话能被服务到的帧率上限就是调度频率的一半,GPU 多快都一样。

```
上限 = 1 ÷ (间隔倍数 × 每轮耗时)   帧/秒,   而要求是 12.5
```

停车协议下"间隔倍数"是 2,去掉之后是 1。六个格里量出来的上限是 20.8、12.5、9.7 帧/秒,对应结果是通过、
不通过、不通过 —— 12.5 之所以不通过,是因为刚好等于要求就等于没有余量,任何一轮跑长一点就漏。
**这三个格里 GPU 都不是瓶颈**,这也正是之前所有冲着算力去的假设全都打偏的原因。

A 改成**在调度线程上同步取件**,就在这个请求即将被停车的最后一刻(`_try_inline_receive`)。一个归属
登记表保证每个请求同时只有一个取件方,文件锁是非阻塞的,整条路径只在 `model_mode == "ar"` 下生效。
56 个会话上:错过截止时间 **20.1% → 0.9%**,卡顿 35 毫秒/轮,迟到的时间线变平。

`VLLM_OMNI_INLINE_RECV_ASYNC` 之所以存在:一个占位符判断让内联取件和 vLLM 的异步调度互斥了,而这里
talker 就是开着异步调度的,所以没有这个开关时这个机制**每一次都被跳过**(量到:0 次命中、23553 次
跳过),而表面上看它是开着的。

### 19. code2wav 保持自己一个进程 —— 这是一个决定,不是一项新增

原版本来就把三段跑成三个进程。`VLLM_OMNI_COLOCATE_STAGES` 是我们加的:为了省 CPU,把 stage 2 作为
一个**线程**放进 talker 的进程里。64 个会话上量出来这一步是错的。两段都是 Python 驱动的,于是在解释器
锁上互相挡:talker 28.5 毫秒的一轮里大部分在等,声码器 71 毫秒里也大部分在等,GPU1 报 50%,而客户端
有 5.9% 的块迟到。这一条留在这里是因为有用的是这些数字 —— 它们说的是"这两段不要放一个进程"。

| | 共进程(`=2:1`) | 各自独立进程(默认) |
|---|---:|---:|
| GPU1 有核在跑的比例 | 50% | **96%** |
| talker 每轮 | 28.5 毫秒 | 14.7 毫秒 |
| 声码器每次调用 | 71.2 毫秒 | 23.0 毫秒 |
| 64 用户 错过/卡顿 | 5.88% / 307 毫秒 | **0.41% / 17 毫秒** |
| 第一声 p50 | 1287 毫秒 | 693 毫秒 |

独立进程的代价是每个编码块多一次真正的进程间传递,表现为中等负载下第一声的**尾部**变差(56 用户:
p99 1800 → 2577 毫秒),而中位数变好(698 → 563 毫秒)。64 个会话上这是"不达标"和"达标"的区别,所以
共进程默认关闭,`VLLM_OMNI_COLOCATE_STAGES=2:1` 是复现那个坏摆法的方式。

### 20 到 25. 部署(`benchmarks/thinker_talker/deploy_2gpu.yaml`)

要对比的参照是原版自己的两卡示例 `vllm_omni/deploy/qwen3_omni_moe.yaml`:它已经把 thinker 放 GPU0、
语音那两段放 GPU1,而且三段本来就都开着 CUDA graph。真正的差异是下面这些:

- **stage-1 `async_scheduling: true`** —— 这是 vLLM 自己的 CPU/GPU 重叠,把 stage-1 每轮时间砍一半。
  它只有和 #18 一起才安全:停车那条路正是它会踩坏的地方,所以这两件事是一起到的。stage 0 保持关闭,
  因为它的到达追加路径会从 `token_ids_cpu` 里读到 −1 哨兵,把这一段打死。
- **三段都用 FP8 权重 + FP8 KV**,在加载时从官方 bf16 权重量化(社区的 block-FP8 权重在这个 fork 上
  产生退化文本)。thinker 权重 59.4 → 31.7 GiB,每 token 的 KV 减半,stage-0 池子大约 6 倍。真实数字
  看启动日志的 "GPU KV cache size" 那行,不要相信估算。
- **CUDA graph 保持开着** —— 这是一个决定,不是一项新增。原版本来就开着;我们早期的单卡配置用
  `enforce_eager` 省显存,那让 talker 在 32 用户时只有 29% 的时间有核在跑。声码器的 graph 封装已知在
  异构的**视频**混批上会出问题,而这里 stage 2 的输入只有音频,连续跑几小时都是干净的。
- **`codec_chunk_frames` 25 → 4。** 声码器一次调用覆盖 4 个编码帧 = 320 毫秒音频,而不是 25 帧 = 2 秒。
  播放流畅度的一切都建立在这个颗粒度上:按 2 秒一块,第一声不可能早于"已经攒够 2 秒语音",而一次调用
  晚了就是一个 2 秒的洞。
- **尺寸。**每段 `max_num_seqs` 64 → **80**(一个会话一个槽,再加上压缩影子必须和它要替换的那个请求
  同时存在的余量);stage-1 显存比例 0.6 → **0.30**(够约 100 万 KV token、每会话约 5 万,因为 GPU1 只
  和声码器共享);每轮 token 预算 thinker 32768 → **16384**、talker 32768 → **8192**(周期性的一段要的
  是短而可预测的轮次,不是最大的轮次);thinker 打开**前缀缓存** —— 会话级常驻请求能吃到它,而原版是
  关的。
- **对应关系用的就是原版的**:thinker 在 GPU0,talker 和声码器在 GPU1。这台机器没有 NVLink,所以跨卡
  的只有 thinker → talker 的文字载荷 —— 这也是预填充/解码分离部署被否掉的原因:要搬的状态就是
  KV cache。

---

### 26. 每步的载荷行改走锁页内存

**原版。** 解码载荷到达时是一个 CPU 张量,由 `torch.frombuffer` 架在共享内存段上——**非锁页**内存。
对非锁页内存做 `.to(device)` 是**同步**拷贝:它要经过驱动的中转缓冲,而且和流对齐,所以**会阻塞发起
它的线程**。talker 每 80 毫秒欠每个会话一步,于是这笔钱每会话每步在调度线程上付一次。

**改之前量到**(176 个音频会话):一行 4 KB 花 **147 微秒**——折合 28 MB/s,而这条 PCIe 路径能做
20 GB/s,所以时间基本全是每次调用的固定开销和等流,不是传输。它是这一段关键线程上最大的单项
(占 py-spy 采样的 32.7%)。

**A 的做法。** 先把这一行拷进 PyTorch caching host allocator 的锁页缓冲,再发**异步** H2D。
安全性由那个分配器保证:在记录到某个锁页块上的拷贝完成之前,它不会把这个块交出去复用,所以跨会话、
跨轮复用不会串数据。消费侧的顺序由流保证——后面每个算子都排在这次拷贝之后。

**效果**(纯音频,回复长度随机,每会话 10 轮,前两轮扔掉):

| 负载 | 卡顿比例 | 每轮卡顿 | 判定 |
|---|---|---|---|
| 168 会话 | 2.92 / 4.67% → **1.75%** | 65 / 87 → 31 毫秒 | 本来就过,现在余量更大 |
| 176 会话 | 27.2% → **2.03%** | 338 → 45 毫秒 | **不通过 → 通过** |
| 200 会话 | 80.9% → 50.3% | 4052 → 2376 毫秒 | 仍不通过 |

最大可服务 **~168 → ~180 个会话**。三件独立的证据指向同一处:每会话每轮的边际成本
745 → 612 微秒(少的 133 与那次拷贝自己的 147 微秒吻合);那一行从关键线程的前八名消失;
同样负载下该线程的忙碌占比从 58% 降到 44%,轮次从 81.4 缩到 68.4 毫秒。

会话数只涨 7%、而每轮耗时降了 16%,原因是判定用的是卡顿的**尾部**,而投递间隔的 p99 改善比中位数
小:异步拷贝是"入队"而不是逐个做完,拿走了一部分单次投递的确定性去换吞吐。

只改了**解码**这条路。开一轮时的预填充接收做的是同一种拷贝,这次故意没动,好让这次改动的效果可归因。

---

### 27. 码本预测器改用固定大小的 KV cache

**原版。** 一帧音频由 16 个码本组成,第一个由 talker 给出,其余 15 个由码本预测器逐个产生。
而每一步都重新前向**整个** 17 位置的窗口(`seq_len = max_seq`),所以批 105 补齐到 128 时,
talker 一轮要做 128 × 17 × 15 = 32,640 个 token 前向。实测 **每轮 35.4 ms 的 GPU 时间**,
是 talker 自己前向的四倍,占一轮的 46.6%。

**A 的做法。** 每步只前向一个位置,注意力对固定 17 个槽做,没写的槽用加法掩码挡掉。
两个性质必须同时成立:算术量降到 1/17,**并且**每步形状不变,一份编译服务所有步。
后半句不是可选的——把每步改成"只算需要的 step+1 个位置"(算术再少 40%)实测**慢 8%**,
因为丢掉了单一形状。

**实测**(184 个音频会话):码本预测器每轮 **35.4 → 约 8.6 ms**,而且随批基本持平
(批 3 到 67 都是 8.4–9.6 ms,即从"按会话增长"变成"每轮一笔固定开销");talker 轮次
76.0 → 34.7 ms;音频迟到 3.6–6.8% → **0.12%**;每轮卡顿 90–180 → 1.2 ms;rtf 1.15 → 2.51;
第一声 p50 873 → 504 ms。最大可服务 **约 180 → 约 200 个会话**。

**等价性是数值等价,不是逐位相同**:两条路径归约顺序不同,bf16 下不可能逐位一致。
验证方式是一个离线脚本:直接构造内层模型,在全部 17 个位置上比较"整窗前向取第 s 行"和
"逐位置 step 前向",相对误差 0.7–1.3%——单次 SDPA 调用本身就有 3.3e-3(bf16 的 2⁻⁸)。
端到端:探针全零、确定性问题答案逐字一致、产出音频的 RMS/峰值/零穿率都在正常语音范围。
`VLLM_OMNI_CP_KV_CACHE=0` 回到重新前向的老路以便对比;`VLLM_OMNI_CP_KV_VERIFY=1` 同时跑两条
路径并打印差值(在 CUDA 图捕获期会自动跳过,那里它的设备同步是非法操作)。

---

## 默认值就是这套配置

2026-08-14 之前,上面每一项默认都是关的(为了和上游一致),那套被测量的配置住在压测脚本里 —— 于是
服务器和"被测的东西"是两个系统。在这个分支上,**客户端连上来只发一个 system prompt,拿到的就是 A**:

`session_scoped_request` 开 · `prefill_frames_on_arrival` 开 · `prefill_audio_on_arrival` 开 ·
`audio_prefill_chunk_s` 1 秒 · `audio_prefill_reserve_s` 1 秒 · `max_frames` 8 ·
`max_frame_width/_height` 640×352 · `frame_jpeg_quality` 90 · `enable_frame_filter` 开(0.95)·
`frame_filter_min_gap` 0 / `max_gap` 4 · `fresh_frame_on_query` 开 ·
`session_roll_at_talker_tokens` 45,000 · `session_roll_history_turns` 8 ·
`context_compression_carry_frames` 关 · `context_compression_target_tokens` 触发线的一半 ·
`context_compression_warmup_timeout_s` 30 秒

引擎侧那几项通过 `vllm_omni/core/sched/runtime_flags.py` 默认打开:`VLLM_OMNI_INLINE_RECV`、
`..._RECV_ASYNC`、`VLLM_OMNI_INLINE_SEND`、`VLLM_OMNI_MAILBOX`、`VLLM_OMNI_STREAM_VOCODER`、
`VLLM_OMNI_T2T_LEAN_DECODE`。注意"没设"会落到"默认打开"上,所以启动横幅如果打印原始环境变量,一个
其实开着的开关会显示成 0。把某一项设成 `0` 就是搭对照实验的方式 —— 关掉一个机制,而不是把基线打开。

有三个数是**机器的属性**,不是某次对话的属性,所以从启动时的环境变量来(`run_engine.sh` 负责传):
`VLLM_OMNI_STAGE0_KV_POOL_TOKENS` 和 `VLLM_OMNI_STAGE1_KV_POOL_TOKENS`(从启动日志的
"GPU KV cache size" 行读),以及 `VLLM_OMNI_ADMIT_MAX_SESSIONS`。它们负责上面两道准入门槛,并决定
自动的压缩触发线。解析结果每个会话打印一次,在 `[session] context compression armed:` 那一行。

---

## 怎么跑、怎么测

```bash
cd benchmarks/thinker_talker
bash run_engine.sh                  # 启动(约 3-4 分钟),自带一轮预热
bash run_cell.sh MyCell 56          # 56 个会话 × 6 轮,全程记录
bash run_engine.sh stop
```

`run_cell.sh` 会写出 `turns.jsonl`、`metrics.json`、`meta.json`(启动时环境里所有的 `VLLM_OMNI_*`)、
`gpu.csv`(两张卡 10 Hz,带时间戳)、`gpu_pmon.txt`(按**进程**的 sm%,因为 GPU1 上跑着两段,汇总数字
说不清是哪一段在占卡)、`engine_slice.log`。然后:`analyze.py` 出那两个数,`sched_steps.py --stage 1`
出轮次间隔的分位数(也就是上面那个容量不等式的左边),`verify_cell.py` 检查"声称打开的机制"有没有在
日志里留下痕迹。

最后这个之所以存在,是因为有两类错误在客户端指标上完全看不出来。**声称的改动其实没跑**:第一次测内联
取件时日志是 `irecv=0/0` —— 那段代码在这个部署根本不走的分支里 —— 结果完美复现了基线,看起来就像"这
个想法没用"。**两次对比的配置其实不一样**:这件事发生了五次,每次都值大约 2 倍的头条数字,其中一次直接
把结论反过来。还有一条在客户端:思考时间必须从上一轮回答**播完**开始算,而不是从最后一个字节到达开始算
(`MU_PLAYBACK_PACED=1`),否则引擎越快下一个问题就来得越早,并发数会悄悄掉 —— 曾经出现过两个都标着
同样 N 的运行,一个实际 28 个并发会话,另一个 49 个。

### 压测客户端不压缩 websocket

`mu_bench.py` 用 `compression=None` 连接。`websockets` 库默认会协商 permessage-deflate,服务端也接受,
所以在这之前每一个音频块发出去要 zlib 压一遍、收进来再解一遍。200 会话下用 py-spy 抓 API server,
**42% 的事件循环时间在 `permessage_deflate.encode` 里** —— 压的是音频 PCM,本来就压不动。
200 会话、同种子、背靠背的 A/B:

| | TTFA p50 | TTFA p99 | 卡顿块比例 | API server 最忙的线程 |
|---|---|---|---|---|
| 压缩开 | 645 ms | 1654 ms | 0.44% | 一个核的 65% |
| 压缩关 | 558 ms | **728 ms** | 0.48% | 一个核的 **34%** |

三个 stage 进程一点没动(99 / 67-68 / 81%),所以变化只发生在 API server 上。还有两个signature确认这是
机制而不是巧合:固定并发(190-200 会话)下,"发出请求→第一个字"在一个会话内随轮次的增长,从 106→396 ms
变成平的 69→88 ms;对照组在 p99 上复现了更早的一次运行,差 6% 以内。

这是**串行**成本 —— 一个事件循环被所有会话共用 —— 所以它表现为尾部,而不是打满:循环平均只有一个核的
65%,却扛着 1.6 秒的 p99。**平均 CPU 中等、尾部很高**就是这种问题的形状;第一次排查时正是因为
"58%,没打满"而把 API server 排除掉了。

**服务端故意不动。**真实浏览器会协商这个扩展,也应该继续省那份带宽(在真实回答上实测能压掉 36%)。
只有压测客户端退出协商,这样容量数字才不会花在引擎根本没要求的压缩上。`MU_WS_DEFLATE=1` 可以加回来,
上面那组 A/B 就是这么跑的。

关掉之后,200 会话下 TTFA p50 的 558 ms 拆开是:**75 ms** 发出请求→thinker 吐出第一个字(整条 web 入站
路径加上 thinker 都在里面),**304 ms** talker 把这个字变成第一个 codec 块,**179 ms** code2wav 加编排
加送达。第一个块固定是 4 个 codec 帧 = 320 ms 语音(`initial_codec_chunk_frames`),所以 TTFA 里一半以上
是 talker 在填这第一个块 —— 想再降 TTFA,要动的是这里,不是 web 层。

---

## 现在到哪了

每会话 6 轮、压缩开着、两张卡都 FP8:

| 负载 | 错过截止时间 | 每轮卡顿 | 第一声 p50 | 第一声 p99 | GPU 有核在跑 |
|---|---:|---:|---:|---:|---:|
| 56 个会话 | 1.17% | 85 毫秒 | 833 毫秒 | 2271 毫秒 | 82-85% |
| 64 个会话 | 6.15% | 364 毫秒 | 997 毫秒 | 3591 毫秒 | 82-85% |
| 64 个会话,到达时间摊开 | **0.07%** | **3.5 毫秒** | 346 毫秒 | 776 毫秒 | — |

第三行是重点:同样 64 个会话、同样的总工作量,只要压缩浪潮不重合就轻松通过。64 个会话上剩下的是
**同时性**问题,不是容量问题。

**量过之后否掉的** —— 记在这里,免得以后又盲试一遍:

- 把 thinker → talker 的文字合成更少更大的载荷,或者按时钟合批:停车一去掉,轮次间隔就不再变化;
- 编码器分离部署:视觉编码器只占 thinker GPU 前向时间的 **10.7%**(在带它的那 15% 轮次上 p50 是
  14.3 毫秒),搬走它省下的不够付一次 PCIe 传输;
- 预填充/解码分离部署:要搬的状态就是 KV cache,而这台机器没有 NVLink。

### 现在音频的上限卡在哪

**第 27 条之后,上限已经不在 talker 了。** 200 个会话:音频迟到 0.55%、每轮卡顿 7.8 毫秒、
第一声 p99 1747 毫秒、没有准入拒绝——通过。240 个会话:迟到仍然只有 1.00%,但第一声 p99 是
3409 毫秒(不达标),而且 thinker 的池子门槛拒掉了 33 个会话,因为
`0.75 × 1,132,672 / 240 = 3540` 低于 4096 的准入下限。所以音频这条 workload 现在被
**thinker** 卡住:它的第一声尾部,以及 stage-0 KV 池子给出的约 207 个会话的硬上限——
不再是语音那两段。

下面这份分解是第 27 条之前的状态,保留是因为它正是定位到码本预测器的那份数据:

184 会话、平均批 105、每轮 76.0 毫秒(`VLLM_OMNI_LOG_MTP_GPU=1`):

| 项 | 每轮 | 占轮次 | 每会话 |
|---|---:|---:|---:|
| **码本预测器前向(GPU)** | **35.41 毫秒** | **46.6%** | **339 μs** |
| talker 主模型前向(GPU) | 8.81 毫秒 | 11.6% | 84 μs |
| 关键线程(CPU) | 33.99 毫秒 | 44.7% | 325 μs |
| 合计 | 78.2 毫秒 | 对实测 76.0 毫秒差 3%,说明没有大块未归因 | |

上限现在在 **GPU**,而且是**码本预测器**,不是 talker:一个 5 层、hidden 1024 的小模型,每会话的
开销是 talker 自己前向的 4 倍。三个原因,都能在配置和代码里对上:

- `num_code_groups=16`,所以**每个编码帧要串着跑 15 个 AR 步**——这是模型结构;
- **不用 KV cache**:每一步重新前向整段增长的序列(长度 2→17),每轮 15,960 个 token 前向,
  而有 cache 只需 1,680 个——**9.5 倍冗余**。包装类的注释说"短序列下多出来的 O(T²) 可忽略",
  这在批=1 时成立,在批=105、每秒 13 轮时不成立;
- `use_cuda_graphs=current_omni_platform.is_npu()`,也就是**在 CUDA 上是 eager**:
  35.4 毫秒算 2176 GFLOP = 61.5 TFLOPS,约为这张卡的 20%。

已排除:那条"每会话单独跑一次前向"的逐行回退路径要求请求带 seed,而这个部署不设 seed。

### 再往上:300 个会话的墙是 GPU1

关掉 websocket 压缩后,200 个会话余量很大:第一声 p99 **728 毫秒**(判据 2000)、
迟到 **0.48%**(判据 5%)。300 个会话崩:rtf **0.83**(生成 1 秒语音要 1.19 秒)、
迟到 78.6%、每轮卡顿 3128 毫秒、25 个用户掉线。总吞吐从 82 只涨到 94 音频秒/墙上秒。

**不是准入。**把 `stage0_admission_floor_tokens` 放到 2048、份额因子放到 0.8 之后,
300 个会话一个都没被拒。实测每会话的 thinker 上下文只有约 1000 个 token,
200 会话只用掉 stage-0 池子的 **18%**——4096 那个下限是实际用量的 4 倍。

**不是显存。**GPU0 的 88 GiB 是启动时按 `gpu_memory_utilization` 一次划走的,与会话数无关;
GPU1 只用 47/95 GiB。

**是 GPU1 的算力。**talker 每轮的时间随并发线性增长(91 个批次点拟合):

```
一轮时间 = 6.1 毫秒 + 529 μs × 同时说话的会话数
```

一轮给批里每个会话产出一个编码帧 = 80 毫秒语音,所以一轮必须在 80 毫秒内跑完,
上限约 **139 个会话同时说话**。300 会话时峰值 176(一轮 104 毫秒,跟不上实时),
200 会话时峰值 74。

那 529 μs 是**等 GPU**,不是 CPU 开销。在 `_preprocess` 同一段区间上同时记墙上时间和
CUDA 时间线时间(事件延后 8 轮读,探针不引入同步;探针是本地的,未提交):

| 并发 | 墙上 | GPU 时间线 |
|---|---:|---:|
| 61–110 | 8.99 毫秒 | 31.45 毫秒 |
| 151–190 | 29.98 毫秒 | 51.39 毫秒 |

GPU 时间线**长于**墙上时间,说明 CPU 排活比 GPU 干活快,GPU 在欠账。

**GPU1 上最大的消耗者是 code2wav,不是 talker。**按进程看占用(`pmon`,300 会话):
code2wav **54%**,talker **28%**。把 code2wav 挪到 GPU0 让 talker 独占 GPU1,同并发
(61–110)下上面那段区间的 GPU 时间线从 31.45 掉到 **16.04 毫秒**(其中 MTP 前向本身
9.55 毫秒)——腰斩的那一半就是 code2wav 占卡的时间。

但**挪卡不是解法**:GPU0 被推到 94%、GPU1 闲到 38%,迟到反而 62.3% → 89.2%。
两张卡怎么摆都有一张先满(68%/88% 对 94%/38%)。
(挪卡这组有干扰:为腾地方把 thinker 显存 0.90 压到 0.78。GPU 时间线那个测量不受影响——
同并发、同一条流上量的。)

**下一个要看的是 code2wav**:GPU 上最大的消耗者,而且从未被 profile 过——
之前所有剖析都对着 stage 1。(测了,见下一节。)

一条测量上的注意事项,写在这里以免重犯:**CPU 占用高不等于在干活**。CUDA 的同步默认
自旋等待,所以"主线程 78% CPU"和"栈停在某一行"对"在算"和"在等"是同一个读数;
采样剖析器看到的是把 GPU 活排进队列的那一行,真正的等待记在下一个同步调用上。
按这个读数逐行优化 talker 的 CPU 路径,做了三轮只拿到 4-5%,已全部撤回。
要分清"算"和"等",只能用配对的 CPU/GPU 计时。

所以那段 GPU 时间里大约九成是可以不做的:序列是 2→17、批大小已知,cache 就是一块固定形状的
缓冲(约 44 MB);配上 CUDA graph,同样的算术应该落在 4–8 毫秒,而不是 35 毫秒。

**还没做完的:**第一声延迟里 thinker 那 352 毫秒没有拆开看内部构成 —— 需要一个"请求 id ↔ 轮次"的
对应关系,现在的日志里没有;第一声 p99 的尾巴也还没完全归因:压缩是首要嫌疑,但那个隔离实验
(同样负载、关掉压缩)还没跑完。

### code2wav:卡顿的原因,一刀砍掉三分之一

**它就是卡顿的原因。**把 code2wav 的计算整个短路掉(`VLLM_OMNI_CW_BYPASS=1`,发等长的
静音,块数、块时长、到达时刻都不变,只有采样点是假的),300 个会话:

| | 基线 | 短路 code2wav |
|---|---:|---:|
| 迟到块比例 | 75.4% | **4.27%** |
| 每轮卡顿 | 2826 毫秒 | **93.6 毫秒** |
| rtf | 0.84(掉队) | **1.22(跟得上)** |

两次都是 275 人接入、2200 个有效轮次,只差这一个变量。

**它的成本是一条直线。**离线单测(两卡空闲、真权重、bf16、形状照抄线上的 25 帧左上下文
+ 4 帧新):批大小超过 8 以后,每多一路同时说话的会话固定 **+1.4 毫秒 GPU**,再大的批也不
摊薄。折算成每秒语音 **4.24 毫秒 GPU**,所以一张卡的上限是 **236 路**同时说话的流。

**时间不在卷积上,在显存搬运上。**它只跑到本卡 bf16 峰值算力的 **14%**(57 / 411 TFLOP/s)。
根源是 Snake 激活 `x + (1/β)·sin²(αx)`:transformers 的实现把它拆成 5 个独立 kernel
(乘、sin、平方、乘、加),每个都要把整个张量从显存**完整读一遍写一遍**。解码器最后一级的
张量是 `[B, 96, 28800]`,B=32 时 177 MB,5 步就是 11 趟显存往返,而融合后只需要 2 趟。
这个运算是每搬 1 字节做 2.5 次浮点运算,这张卡的收支平衡点是 200 次——纯访存受限,
减少搬运是唯一有效的手段,运算本身增不增加几乎无所谓。

**融合内核仓库里早就有,只是没接上。**`common/snake_activation.py` 的 `SnakeBeta` 带一个
Triton 融合实现,一趟读一趟写,中间结果留在 fp32 寄存器里。Qwen3-Omni 的 code2wav 有 29 个
Snake,其中 **28 个**来自 transformers 的 `Qwen3OmniMoeCode2WavDecoderBlock`,只有最后
那 1 个是融合版。建模时把这 28 个换掉(参数名和形状完全一致,权重加载不受影响);启动日志的
"Precomputed exp caches for N SnakeBeta activations" 从 1 变成 29,可以直接验证是否生效。

**离线效果吃满了理论上界。**B=80 每次调用 **108.7 → 72.0 毫秒**;把 Snake 整个删掉是
**71.99 毫秒**——融合后和删掉一样快,这条路上没有剩余空间了。一张卡的上限 **236 → 356 路**。

**精度不降反升。**和同一个模型的 fp32 输出比信噪比:transformers 版 32.36 dB,融合版
**32.73 dB**。原因是原版每一步的中间结果都要落回 bf16(5 个舍入点),融合版只在最后写回时
舍入一次。顺带说明 bf16 本身在这个解码器上对 fp32 只有约 32 dB,两个 bf16 版本之间那点
差异是 bf16 的噪声,不是融合引入的。

**线上 300 个会话的效果:**

| | 基线 | 融合 Snake | 短路(上界) |
|---|---:|---:|---:|
| 迟到块比例 | 75.4% | **40.4%** | 4.27% |
| 每轮卡顿 | 2826 毫秒 | **1077 毫秒** | 93.6 毫秒 |
| rtf | 0.84 | **1.01** | 1.22 |
| 第一声 p99 | 4628 毫秒 | 3886 毫秒 | 3253 毫秒 |
| code2wav 占 GPU1 | 52.8% | **39.0%** | 10.5% |

拿到了可得收益的一半:(75.4 − 40.4) / (75.4 − 4.27) = 49%。腾出来的卡给了 talker
(34.5% → 39.5%)。

**但 300 个会话仍然不过,而且不是 code2wav 的问题。**看上界那一列:即使把 code2wav 的计算
完全删掉,第一声 p99 还有 **3253 毫秒**,判据是 2000。第一声发生在音频开始生成之前,GPU1 的
算力碰不到它。**继续优化 code2wav 只能改善卡顿那一条,救不了第一声。**

**还剩的两把刀**(都只作用于卡顿侧):把 `codec_chunk_frames` 从 4 提到 8,卷积窗口从
"15 帧出 4 帧"变成"19 帧出 8 帧",少 1.58 倍工作量,只改配置,首块不变所以不影响第一声;
再往下是给每个卷积层做流式状态缓存,窗口 15 帧 → 4 帧,少 3.75 倍,但要为每个会话保存
约 30 层的卷积状态,和批处理、CUDA graph 都要重做。

一条测量上的注意事项:**离线测这个必须关梯度**。融合路径有 `not torch.is_grad_enabled()`
的门槛,开着梯度根本走不到融合内核;而且 autograd 会把 1920 倍上采样分辨率上的中间激活全部
留住,直接爆显存。生产在 `inference_mode` 下跑,离线测量要对齐。

---

## 代码在哪

| 什么 | 在哪 |
|---|---|
| 会话层、准入、压缩、到达预填充 | `vllm_omni/entrypoints/openai/video_stream_base.py` |
| 相似帧过滤的上下界 | `vllm_omni/entrypoints/openai/video_frame_filter.py` |
| 媒体解码子进程 | `vllm_omni/entrypoints/openai/media_pipeline.py`、`media_worker.py` |
| 内联取件 / 内联发送 | `vllm_omni/distributed/omni_connectors/transfer_adapter/chunk_transfer_adapter.py` |
| 常驻信箱 | `vllm_omni/distributed/omni_connectors/connectors/shm_mailbox_connector.py` |
| 引擎侧开关 | `vllm_omni/core/sched/runtime_flags.py` |
| 精简解码载荷、talker 只收文字 | `vllm_omni/model_executor/stage_input_processors/qwen3_omni.py` |
| 带窗声码器 | `vllm_omni/model_executor/models/qwen3_omni/qwen3_omni_code2wav.py` |
| 探针(`[SCHED-STEP]`、`[STEP-GPU]`、`[MTP-GPU]`、`[ENC-GPU]`、`[PF]`、`[SEG-CYCLE]`) | `omni_ar_scheduler.py`、`gpu_model_runner.py`、`gpu_ar_model_runner.py` |
| 部署、运行脚本、指标 | `benchmarks/thinker_talker/` |
| 客户端与多用户驱动 | `benchmarks/live_agent/web_client/` |
