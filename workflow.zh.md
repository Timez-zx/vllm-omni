# 我们给 vllm-omni 加了什么

目标:让一个人能开着摄像头和麦克风,和模型**连续聊很久**,而且每次回答都很快。

原版能做到"聊几轮",但聊久了会出问题。下面按加入顺序,一个改动一节,每节都是:
**场景 → 原来会怎样 → 现在怎样**。

代码就在本仓库,分支 `live-agent`。新能力默认都是**关着的** —— 不配置的话行为和原版一样,**只有一个例外**:第十节改的是"一个模型阶段交给另一个阶段什么",它**默认是开的**,用 `VLLM_OMNI_TALKER_TEXT_ONLY=0` 关掉。

---

## 改完之后,能做什么

| 能力 | 一句话 |
|---|---|
| 长会话不再重复劳动 | 聊到第 20 轮,只处理新画面,不再把前 19 轮重看一遍 |
| 画面自动变小 | 手机传 1280×720,服务端自己缩到 640×352 再给模型 |
| 取帧节奏可控 | 画面静止时不会"看不见现在",画面乱动时不会把提示塞满 |
| 对话可以无限继续 | 快到上限时自动换一个底层请求,用户察觉不到 |
| 撞上限之前有预警 | 每轮打印距离上限还有多远,以前完全看不见 |
| 出故障会喊 | 三类"静默卡死"现在会自己报出来,以前日志里什么都没有 |

---

## 一、聊得越久,回答越慢 —— 因为前面的话被重复算了

*2026-07-31 00:07 – 01:24 · `e3ebb356` 一个会话一个请求 · `a53a0834` 排空帧缓冲 · `fa52c693` 拒绝轮次重叠*

**场景。** 用户举着摄像头,聊了 20 轮。

**原来会怎样。** 每问一句,系统都当成一个**全新的任务**来办 —— 前面 19 轮的画面和对话,得**从头再走一遍**,才能回答第 20 句。聊得越久,每一句就越慢。

**现在怎样。** 整个对话从头到尾算**一个任务**,每轮只把**新增的那几帧和这句话**追加进去。前面的东西一直留在显存里,不用重算。

**效果。** 语音那一段几乎不再因为"聊得久"而变慢。原来聊到 3 万多字的上下文时,是最慢的时候;现在反而比"把历史全丢掉"的做法更快 —— **留着历史比丢掉更便宜**。

---

## 二、手机传过来的画面太大了

*2026-07-31 00:01 / 00:37 · `d1abbf8f` 到达即降采样 · `6ec99006` 挪出音频通道*

**场景。** 客户端摄像头是 1280×720,每一帧都原样传给模型。

**原来会怎样。** 模型按原尺寸处理每一帧。一帧 720p 的画面要拆成 880 个"块"来算;而这些块的数量直接决定了要花多少时间。

**现在怎样。** 帧一到服务端就先**缩小**(比如缩到 640×352,变成 220 个块),然后才给后面所有环节用。长宽比保持不变,只缩不放。

**有一个坑必须避开。** 缩放必须放在**最前面** —— 判断"这帧和上一帧像不像"的过滤器、缓存、以及最终喂给模型的内容,必须看到**同一个版本**的画面。否则会出现"过滤器拿大图比较、模型读小图"这种对不上的情况。

**还有一个性能坑。** 这个缩放一开始是"顺手"做的,结果占用了**同一条负责把声音推给用户的通道** —— 于是缩图的时间直接加在别人等回答的时间上。后来把它挪到旁边单独做。

---

## 三、画面静止时"看不见现在",画面乱动时"塞满提示"

*2026-07-31 00:04 · `f4837245`*

**场景一。** 用户对着一面白墙不动,过了半分钟才问一句。
**场景二。** 用户在走动,画面每一帧都不一样。

**原来会怎样。** 系统靠"这帧和上一帧像不像"来决定要不要留。于是:
- 白墙那种情况,**可能几十秒一帧都不留**,模型对"现在"是瞎的
- 走动那种情况,**一口气留十几帧**,把提示塞满,回答变慢

**现在怎样。** 给"两次保留之间隔多少帧"加了**下界和上界**:
- 下界:再怎么变化,也不会连续猛留
- 上界:再怎么静止,隔够了就必须留一帧

判断像不像的逻辑**一点没改**,只是限制它"多久可以表态一次"。按帧数算而不是按秒,所以客户端帧率变了行为也不变。

---

## 四、聊到某一轮,对话突然停住了

*2026-07-31 02:42 · `b866eb15`*

**场景。** 聊到第 27 轮,忽然:模型还在**打字**(文本一直出),但**一句话都不说**。等了两分钟,日志里**一个错误都没有**。

**为什么。** 这是最难查的一个。负责"说话"的那一段有一个容量上限,撞上去之后:要么整个进程被打死,要么**每一轮都被悄悄跳过**,谁都不报错。

**关键在于:满的不是你以为的那个东西。**

> 那个上限装的不是"聊天历史",而是**说话那一段自己的账本** —— 里面既有每轮新增的内容,也有**它自己生成的语音数据**。而话说得多的一轮,语音数据是更大的那一块。
>
> **所以撑爆的速度,取决于"模型说了多少话",不是"聊了多少轮"。**

这解释了一个反直觉的现象:**回答简短时 50 轮没事,回答啰嗦时 27 轮就崩** —— 轮数多的那次反而更安全。

**现在怎样。**

- **每轮都打印"现在用了多少 / 上限是多少"**。以前这件事在崩掉之前**完全看不见**,没有任何现成指标会提示快到了。
- 可以设一个额度,到了就**干净地结束对话**并告诉客户端原因,而不是让某个环节崩掉或者悄悄卡住。

这只是**护栏**,不是解决 —— 触发了对话就结束了。真正的解决是下一节。

---

## 五、让对话能无限聊下去

*2026-07-31 03:46 · `4502b142`*

**场景。** 用户想一直聊,不想因为"系统内部满了"被打断。

**现在怎样。** 快到上限时,系统在背后**换一个新的底层请求**:
1. 把旧的退役
2. 开一个新的
3. 把**最近几轮的对话文字**当作开场,先说给新的听
4. 手上还没用的画面,跟着这一轮一起给它

**用户完全察觉不到**,只是那一轮回答慢一些(要重新热身)。

**换过去之后丢什么。** 只有**文字**过得去,**画面记忆过不去**。所以模型仍然看得见"现在"(手上的帧跟着过去了),丢的是**更早看到过的细节**。

**为什么"带过去的历史"必须有限。** 带过去的内容要在新请求里重新读一遍。如果不设上限,**每换一次带的东西就更多,最后光是这些开场白就又把上限撑爆了** —— 那就等于没换。所以默认只带最近 8 轮。

一句话:**时间上可以无限,记忆上必须有限。**

**效果。** 40 轮连续对话、中途换了 2 次,全程没崩没卡。同样的设置**不换的话,第 27 轮对话就终结了**。代价是换的那一轮慢约 3 倍,平摊到每轮约多几十毫秒。

---

## 六、系统明明有活干,却以为自己闲着

*2026-07-30 23:59 – 07-31 12:32 · `c27654de` 直接数真实状态(最终修复) · `b1f423f1` 待处理数据算作有活 · `1aed4032` 清掉已取消的任务 · `9fdee244`、`b7392f3c` 两次未命中的尝试*

**场景。** 换请求之后那一轮,对话又卡住了 —— 而且这次连"正在忙"的迹象都没有。

**为什么。** 系统内部有个计数,用来判断"现在还有没有活要干"。这个计数**在某些情况下会漏减**,越用越偏。而**偏一个,就正好抵消掉一个真实的任务** —— 于是系统认为"没活干",就**睡过去了**;而活其实就在手上,永远没人接。

打个比方:门口的人数牌坏了,牌上写着"里面还有 1 位没走",实际上早走了;于是新来的客人被抵消掉,值班的人以为没人,关灯下班了。

**现在怎样。** 不再相信那个计数,**每次直接去数一遍**当前真实的状态。

顺带还有两处同类问题:一个是"已经拿到手、等着处理的数据"没被算作"有活干";另一个是已经取消的任务留在队列里没清掉,被后面的逻辑捞出来算,直接把进程打死。

**一个值得记的判断。** 中间我试过"把那个坏掉的计数**修正**过来",结果更糟 —— 反而把它推成了负数。**报告别人的账本坏了是有用的,自己动手去改它不是。**

---

## 七、让故障自己开口

*2026-07-31 01:19 – 04:21 · `921314e8`、`625ad170` 卡死探测 · `53a709a5`、`241aa5fd` 状态转储 · `cd29fe65` 进出记录 · `713e5361` 空转上报*

**场景。** 前面那些卡死,共同特点是:**日志里什么都没有**。系统安静地停在那里,分不清是"正常空闲"还是"卡死了"。

**现在怎样。** 加了几组自检,平时不吭声,出事才喊:

- 某个环节**手里有任务但一直没进展** → 报出来,并把当时所有状态打印出来
- 某个环节**从"有任务"变成"一个都没有"** → 报出来(说明任务在到达它之前就丢了)
- 每次任务的**进入和结束**都记一行,结束时说明原因("正常完成"和"被取消"成因完全不同,以前长得一样)

**这里有一条经验,比任何单个 bug 都值钱。**

原来所有自检都挂在同一段循环的**末尾**。循环一停,它们**全体沉默** —— 而这种沉默读起来像"一切正常",恰恰相反。

最后定位到那个坏计数,靠的是一个**放在"会停的那段"之外**的探针:它盯的正是"要不要睡过去"这个决定本身。

> **规矩:至少留一个探针在"会停的区域"之外,并且让它报告"我在、我看到什么",而不是靠"没有报错"来推断没事。**

---

## 八、测量工具也搬进了仓库

*2026-07-31 13:40 · `404b16b0`*

跑实验的脚本、出报告的脚本、图,全部放进 `benchmarks/live_agent/`。理由很简单:**它们测的就是这份代码,得跟着一起走版本**,分开放迟早对不上。

---

## 九、有些数字本来是假的

*2026-07-30 23:36 / 23:41 · `d6ba9563` 每段都报输入量 · `38589f1f` 真的去测段间传输*

**场景。** 想知道"说话那一段"每轮拿到多少输入 —— 因为怀疑它是慢的主因。

**原来会怎样。** 两件事让这个问题没法回答:

- 统计输入量的代码被一个条件挡着,**只统计第一段**。说话那一段的输入量**根本没有数**。
- 段与段之间传数据的耗时和字节数,是**写死的 0** —— 不是没测准,是压根没测,填了个 0 上去(原作者自己留了 TODO)。

**现在怎样。** 每一段都报自己的输入量;段间传输的构建和发送都真的计时,按需打开。

**为什么把它放最后说。** 它不改任何行为,用户完全感觉不到。但**前面每一节的结论都依赖它** —— 没有这两个数,"说话那段是不是主因"这种问题只能靠猜。

---

## 十、念稿子的那部分,一直背着它从不看的画面

*2026-07-31 16:45 · `796795ac`*

**场景。** 对话不能无限继续:负责发声的那部分迟早会装满,整个请求就得换掉(见第五节)。所以真正的问题是 —— **到底是什么把它装满的?**

**原来会怎样。** 两样东西在填:你给摄像头看的东西,和模型自己说的话。第一样是意外之处:**负责念稿子的那部分,画面的每一小块都要占它一格。** 一张清晰的画面就是 880 格。而它**根本不看画面** —— 它的活只是知道该念哪些字。这些格子还永远不还。

*举个真实的例子:* 某一轮里有三张画面,念稿子那部分背着 **817 格**。其中真正是文字的大约 24 格。剩下全是它用不上的画面和录音。

**现在怎样。** 画面和录音**直接不给它了**。只给文字:你问了什么,以及它接下来要说什么。同样那一轮,**从 817 格降到 33 格** —— 少了大约 **25 倍**。

**为什么认为这样是安全的。** 一条借来的,一条量出来的:

- 另一个团队用同样思路做的模型(MiniCPM-o 4.5),给它念稿子的那部分**只给文字,一张画面都不给**。说明这个设计本来就不需要。
- 这里实测,两边各五轮:**每一轮都照样出声**,两种设置都是。而且两边的账每次都对得上 —— 这一点最关键,因为一旦对不上,是**不会报错的**,只会悄悄念错。

**还不知道的两件事。**

- **声音听起来有没有变。** 五轮、每边一次对话、**没有人真的去听**。这个测试能查出"它不说话了",查不出"它语气有点平"。而且这个模型当初**是带着画面训练的**,所以细微的退化是有可能的。
- **对话现在能撑多久。** 还没测出来。测的时候新设置下模型碰巧啰嗦了一倍,而**模型自己说的话也占同一块地方** —— 多说的那些把省下的吃掉了一半。得把模型的随机性关掉重跑一次,让两边说出一样的话。

**能退回去。** 一个设置就能恢复原来的行为。**故意留着的** —— 不留,以后就没有基线可比,这件事就只能靠回忆了。

---

## 十一、现在可以用浏览器和它说话了

*2026-07-31 19:00 – 20:30 · `2452f4dd` 把这些工作搬到上游 main 上 · `ccff770e` 浏览器客户端 · `5dfc170d` 一份装得下的配置*

**场景。** 你想真的和它聊一次 —— 自己的摄像头和麦克风,它的声音回来 —— 而不是用测试脚本去驱动它。

**原来会怎样。** 只有测量用的客户端。它们从磁盘上的文件里读帧和音频、按固定节奏发、而且**用文字提问**。出数字很好用,但完全不能回答"和这东西说话是什么感觉"。

**现在怎样。** 一个网页。采集你的摄像头和麦克风、持续推上去、把回答播出来。服务器和网页各一条命令,只转发一个端口,而且 `localhost` 算安全上下文,所以浏览器给摄像头麦克风权限**不需要配任何证书**。

大部分**不是从零写的** —— 上游给另一个模型做了一个浏览器客户端,它的麦克风采集和样式表直接原样用了。真正要重写的是"和**这个**服务器说话"那一段,因为两边协议有三处不同,而且每一处都是**静默失败**:

* 音频**上去**是裸采样,**下来**是每块一个完整的声音**文件**。把这些文件首尾相接,等于每一块都往音频里塞 44 字节的文件头 —— 听起来是爆音,而你会怪模型。
* 画面在这边是**独立的消息**,不是挂在音频上的附件。
* **必须有个东西说"我说完了"**,而在这个模型上,这个东西只能是网页。见下。

**要诚实的那部分。** 这个模型**没有办法自己决定什么时候开口** —— 它的词表里没有这个选项,服务器也从不自己开始回答。所以由网页来定:要么它发现你安静了 0.7 秒,要么你按住一个按钮。**网页上写明了这一点**,因为这个仓库里**另一个**模型是自己做这个决定的,拿两者对比的人有权知道到底是谁在驱动。

**效果。** 先在不用浏览器的情况下验证,这样结果说的是服务器而不是某一台笔记本:五轮,每一轮都出了文字和语音,第一声在 **340–376 毫秒**。这和之前量到的 513 毫秒是同一量级,让人放心,但**不是同一个实验** —— 引擎版本不同,并发槽位也少了。

**还有一个差点漏掉的 bug。** "有没有出声?"这个检查通过了。然后换成更长的回答再问一次,答案没变:**34 个字符的回答和 104 个字符的回答,都只送来 0.22 秒的声音。** 只有第一小段语音到了网页,后面的做出来了但丢了。又跑了一遍、把我们自己最新的那个改动关掉,还是 0.22 秒,**所以那个改动不是原因** —— 丢失发生在这个分支继承来的那条路上。**记录下来,没有绕过去**,而且在修好之前,网页里的音质不值得去听。

---

## 十二、画面现在是"你还在说话时"就被看懂的

*2026-08-01 · `a9e4e455` 功能本身,以及压在它底下的两个 bug*

**场景。** 你举着摄像头说几秒钟话。这几秒里画面一直在进来。

**原来会怎样。** 每一张画面都**先放在一边**,等你说完话,才一起转成模型能读的形式 —— 而这份活正好挡在你的回答前面。也就是说,画面的钱是在**最糟的时刻**付的:问完之后、你正在等的时候。

**现在怎样。** 每张画面**一到就转**,趁你还在说。等你说完,这份活已经干完了,只剩问题本身。

**比省下的毫秒更重要的一点。** 老做法意味着每轮多一张画面就多等一会儿,所以摄像头一直被故意压得很慢。一到就转把这个联系切断了:**画面发得更密,不再需要你多等。**

**这件事的代价,而这才是值得读的部分。** 打开它会**直接把语音引擎打死**,每一次,都在第一句回答上。一共试了**五次**,而前四次**以完全相同的方式失败** —— 这恰恰是最误导人的线索:看起来像"同一个原因还没找对",其实是**两个毛病同时发生**,而第一个会在第二个能被看见之前就把进程杀掉。

> **毛病一。** 模型吐出一个词的时候,系统会先在该放这个词的位置写一个**占位符**,下一步再从一份短命的副本里把真词填回去。那份副本**只会送给"上一步还在忙"的请求**。我们那个"现在就把这张画面看懂"的活会把语音那部分**短暂地放到一边** —— 于是它回来的时候真词已经没了,只剩占位符。语音那部分拿着这个占位符去查一张它不属于的表,进程就死了。
>
> **毛病二。** 看画面那一步交给语音那部分的是一个**形状不对的包裹** —— 内容是"这里有一大段新材料",标签写的是"这里多了一个词"。在毛病一修好之前根本看不见它,因为崩溃总是先到。

**由此得到两个习惯,都是硬碰出来的。**

*不要托人带话,要直接看东西本身。* 我们试了**三次**给那份工作**贴标签**,让后面的环节能认出它,用了三条不同的通道。三次**都读到空的**,而且悄无声息 —— 承载标签的那个字段早被下一份工作覆盖了。最后奏效的做法是改问一个关于眼前这份工作的问题:*这一步是不是吃进了一大堆新材料、然后就停了?* 如果是,那就没有什么要念出来的。这个事实**丢不掉**,因为它根本没有被"带"到任何地方。

*日志里少一行,不算证据。* 有两次,我们写的检查是靠**某条消息不出现**来确认事情,两次都错了 —— 代码根本没跑到。现在这里所有的检查都改成靠**出现**来确认:那条消息必须打出来。

**它花了什么,说实话。** 四次错误的尝试,每次都要完整重启一遍引擎,之后才把做法从"猜一个原因然后验证"换成"一次把整条路读完、让代码自己说"。这个教训可以推广:**当几个完全不同的修法以一模一样的方式失败时,别再找那一个没找到的原因,该怀疑有两个。**

**干净的数字,终于有了(2026-08-01,在第十三节全部修复之后重测)。** 每轮画面翻三倍,**关着要多付 41 毫秒,开着只多付 10 毫秒** —— 对帧率的敏感度约低 **4 倍**;回答时间的波动从 43 毫秒收窄到 8 毫秒,对"可预测性"这个论点来说后者更要紧。之前的 **16 倍**是在"有些回答被记到错的轮次上"时量的,虚高,撤回。这正是"记账修好后必须重测"的意义:效果是真的,但只有最初宣称的四分之一。

---

## 十三、声音发糊、话说一半被切 —— 一个症状,三个毛病

*2026-08-01 · `9624cac1` `3093205d` —— 用耳朵听出来的,由一次五路并行的代码与日志审计钉死*

**场景。** 开着"画面一到就看懂"(第十二节),和它聊上几轮。第一句回答没问题;从第二三句起,声音开始发糊,有时话说到一半就断了,再也没有下文。

**一句话背景。** 每张被保留的画面都会派生一个小任务:*"看一眼这张图,什么都别说。"* 问题在于,"别说"这半句**只有负责看的那部分收到了**;负责**说**的那部分把每个小任务都当成要回答的东西:哼几声没有内容的音频,然后宣布一句**"我说完了"** —— 货真价实的宣布,和真回答结束时是同一种。

**毛病一 —— 这声宣布认错了主人。** "我说完了"从语音引擎传回前台要走大约一秒。如果你的下一个问题恰好在这一秒里问出去,前台就把这声宣布当成**你的回答**说完了 —— 于是半句话就收工。真正的回答稍后到达,被判"当前没有轮次在进行",整段扔掉。有一场会话就这样丢了 **27.5 秒**真实语音;日志里每一个坏掉的轮次都正好压在这种碰撞上,而健康轮次一个都没有。

修法不是更聪明的标志位 —— 在"东西到达那一刻"读标志位,恰恰就是输掉这场竞速的原因。修法是一本**提交台账**:活儿从一扇门进引擎、按同样的顺序干完出来,所以第几声"说完了"就属于第几个活儿,没有例外。看图任务的哼哼和宣布现在被点名丢弃;只有你问题自己的那声宣布才能结束你的轮次。

**毛病二 —— 接缝不再缝了。** 声音是一小块一小块拼出来的,每块都要和上一块的尾巴缝在一起 —— 像贴墙纸要对花纹。缝合算术里有一个**从不清零**的计数器,却被拿去丈量一张**每轮都清空**的清单。结果从每场对话的第二轮起,每句回答的第二块都是**完全不带重叠**硬拼上去的 —— 那道缝就是你听到的"糊"。同一套错误算术碰到很短的一段时,还会算出负的长度,把那段音频整个扔掉。会在正确时刻清零的计数器代码库里本来就有,兄弟模型的代码早就在用。

**毛病三 —— 哼哼没有上限。** 前两个修好才看得见:那些无内容的哼哼是**不设长度限制的**。有一个任务哼了 **50 秒**,而说话那部分一次只干一个活,下一句真回答的*声音*只能排在它后面 —— 文字 2 秒就好了,声音 52 秒才响。现在,凡是到达时没带任何内容的活儿,最多出一个音就被叫停。叫停机制也是本来就有的,只是从来没接到说话这一级上。

**这一轮添进清单的习惯。** 第五节和第十二节的结尾都是"是两个毛病,不是一个"。这是第三次 —— 次数本身就是教训:当一个症状在看起来正确的修复之后仍然存在,下一个假设不该是"修错了",而该是"下面还压着一个"。另一条也值得留下:决定性的证据早就躺在旧日志里 —— 几周历史中每个坏轮次都开着这个功能,每场关着它的会话都干干净净。一场没人需要做、只需要去读的天然实验。

**验证过,不是推断。** 按浏览器的真实节奏对着运行中的引擎重放 —— 静止画面逼出每 2 秒一个看图任务,八个问题专门掐在碰撞窗口里发。八轮全部完好,每声宣布都被正主认领,上限在每个任务上布防又解除,50 秒的尾巴消失。同样的压力在修复之前,几秒钟就打断了第二轮。

---

## 十四、十六个人同时用 —— 等待扛住了,寿命没扛住

*2026-08-01 首测(bf16)· 2026-08-02 换 FP8 引擎后全部重测 · `e91735b3` `cd399ae0` `ba64c261` `73131a2b` —— 15 格矩阵:1/2/4/8/16 个用户 × 三种画面*

**场景。** 前面十三节都是一个人用。这一节问云上的问题:N 个人同时压在同一张卡上,等待会恶化多少、多快?每个模拟用户都是一条真连接,走浏览器同一扇门,每秒发 2 帧真实视频,进行 30 轮闭环对话(问、听完整个回答、想几秒、再问)。画面用七月研究的同三套:静止屏幕、说话人、手持行走。每一格都在刚启动的干净引擎上开跑;回复长度被提示词钉死(15 格全部是中位 27–29 字符),所以等待的差异只能来自人多。

**量出来的等待**(FP8 引擎,全部 15 格满轮跑完、无一死亡。三个数依次是 P50 / P95 / P99,毫秒。P99 剔除了每格开机后的第一轮 —— FP8 冷启动首轮要 2–4.5 秒,是开机付一次的钱;另外每格样本 60–480 个,P99 应读作"见过的最坏几轮",不是严格分位数):

| 用户数 | 静止屏幕 | 说话人 | 手持行走 |
|---|---|---|---|
| 1 | 380 / 454 / 499 | 385 / 473 / 496 | 399 / 517 / 551 |
| 2 | 439 / 695 / 695 | 421 / 743 / 743 | 453 / 902 / 902 |
| 4 | 575 / 794 / 813 | 614 / 797 / 832 | 673 / 977 / 1,048 |
| 8 | 693 / 965 / 1,074 | 700 / 932 / 985 | 839 / 1,317 / 1,406 |
| 16 | 783 / 1,250 / 1,416 | 798 / 1,069 / 1,121 | **1,405 / 4,673 / 5,368** |

**十六个人的慢,到底慢在哪。** 表里唯一难看的格子(手持 × 16)不是抖动,是匀速恶化:第 2 轮 0.9 秒,第 30 轮 3.3 秒,最慢的 5% 轮次全部落在第 22 轮之后。把每轮切成两段、只看绝对毫秒数(16 人,早期 → 后期,P50/P95):

| 段 | 早期(2–8 轮) | 后期(18–25 轮) |
|---|---|---|
| 想字(提问 → 第一个字) | 202 / 541 | **1,151 / 2,404** |
| 出声(第一个字 → 第一声) | 686 / 954 | 1,297 / 2,216 |

再把最慢 5% 轮次发生的那几秒的硬件采样单独拎出来:**SM(算力单元)93% 打满,显存带宽只用 18%**;按进程拆,思考引擎在慢轮里独占 64% 的算力(中位轮 50%),说话引擎反而从 30% 掉到 21% —— 它不是变慢了,是**抢不到卡**。所以两段一起变慢,根子是同一个:思考引擎把算力吃光了。

**为什么图片多了,瓶颈是算力而不是显存带宽。** 引擎里只有两种活:

- **prefill(消化输入)**:一帧画面变成 220 个 token,**一批**进引擎 —— 全部历史从显存搬出来**一次**,220 个 token **每个**都拿它做一遍乘法。搬运 ×1,计算 ×220;
- **decode(生成回答)**:一次只产 1 个字 —— 同样搬全部历史,却只做 1 份计算。搬运 ×1,计算 ×1。

这张卡的体质是:每搬 1 字节,算力配得起约 110 次乘法。decode 每字节只用 1–2 次(带宽先堵,这是"大模型推理是带宽瓶颈"这句老话的来源);prefill 每字节要 ~350 次(算力先堵)。给一个具体的账:历史 4.5 万 token 时,一帧的 prefill 要**搬 3 毫秒、算 40 毫秒** —— 计算是搬运的 13 倍,搬运车间干完就闲着。而我们的负载里,每轮回答只有 ~30 个 token(decode 型),涌进来的帧却有 ~1,500 个 token(prefill 型)—— **96% 的活是 prefill 型**,整张卡表现出 prefill 的体质:SM 打爆、带宽闲逛。总需求一个乘积说尽:**人数 × 每秒帧数 × 每帧的历史厚度**。前两个因子固定,第三个逐轮上涨 —— 16 人高动态在第 ~13 轮越过产能线,之后全是排队。

**反例恰好证明了这条规律:没有图片的 duplex,先崩的是带宽。** 我们在 Moshi(纯语音全双工模型)上做过多用户测试:它没有任何"成块进上下文"的输入 —— 声音按节拍逐步进,每 80 毫秒一步、每步 1 个时间片,负载 100% 是 decode 型,每字节只配 1–2 次乘法,而且是稠密模型、每步全量权重都要搬 —— 多用户下**显存带宽先出问题**,和这里正好相反。两套系统、两面相反的墙、同一个判据(每字节配几次计算)统一解释。一句话:**实时代理一旦长出眼睛,瓶颈就从显存带宽迁移到算力**;药方也随之不同 —— 带宽墙的药是"少搬字节"(量化、加大批次),算力墙的药是"少做乘法"(历史封顶、按乘积准入),互相不通用。

**墙(bf16 时代的历史,FP8 重测中已不再出现)。** 每场对话的上下文只涨不缩,而思考那级的记忆池是启动时定死的,人多池子不变大。于是池子会在**可以算出来的轮数**耗尽:池子容量 ÷(人数 × 每轮增量)。增量由一个数决定 —— 过滤器每秒保留几张画面 —— 这个公式命中了矩阵里的每一次死亡,包括一次开跑前写下的预测(16 人 × 手持:预测第 4 轮上下,实测 3–5 轮)。轻画面保留 25% 的帧,8 个人第 ~18 轮死;手持保留 56%,**4 个人也是第 ~18 轮死** —— "四个人是安全的"说的是摄像头,不是卡。

**FP8 是怎么拆掉这堵墙的。** 把思考模型的权重和 KV 都转成 FP8(装载时由 vLLM 现转**官方**权重;说话和波形两级保持 bf16),记忆池从 10.7 万涨到 **73.2 万 token**,上面整张表就是换血后的全量重测:15 格无一死亡,文字与 bf16 逐字一致,延迟持平。路上三个教训:社区的预量化检查点能加载但说胡话(弃);0.74 的显存配额是按 bf16 调的,FP8 算子的工作区把余量吃穿导致 8 人 OOM —— 降到 0.70,用 8 万 token 的池子换 4 GB 瞬时工作内存,这才是并发数的真约束;人耳听感已过(正常普通话对话确认),但出现过**一次**会话级怪事 —— 某个会话从第一句起整场说粤语,新开会话即恢复,机制指向"第一句语音的语言判断落在边界上、随后被会话历史锁定",复发时保留现场再查。

**三次崩溃的原因。**

1. **8 人集体死锁 —— 设计问题:被挂起的会话,丢失的信号没有补发机制。** 池满时调度器把会话请求挂起腾地方,这是正常操作;但"这句话说完了"的信号只投递一次,挂起期间错过就永远没有。回合因此永远关不上,之后的提问全被"上一轮还在进行"拒绝。健康检查只测 HTTP、不测音频,死锁全程显示"正常"。**延迟问题变成了寿命问题。**
2. **16 人引擎崩溃 —— 实现问题:显存配额不管运行时峰值。** `gpu_memory_utilization` 只决定记忆池划多大;16 路同时预填画面的瞬时激活内存没有任何参数约束,峰值一超过剩余显存,stage-0 直接 CUDA OOM,进程死亡。
3. **2 人同批崩溃 —— 实现问题:多请求拼批代码从未运行过**(已修,`e91735b3`)。stage-2 按"整批总长度能否被 16 整除"选处理路径;批里混入一个 1-token 短段时总长不整除,代码把 N 个请求拍成一行、又按 N 个去切 —— 下标越界,引擎崩溃。修复:每个请求各自补齐长度。同段代码两个连带问题:有请求缺"接缝"字段时**所有人**的接缝参数被清零(已修,同一提交);大小不同的音频块同批时接缝错 ~23 毫秒(未修,基准期间不改音频输出)。

---

## 十五、上下文压缩 —— 换本子这件事,用户感觉不到了

*2026-08-02,提交 `0b37129c` / `22ce6ea6`。*

**要解决的问题。** 聊得越久,模型的记忆本越厚。厚了有两个坏处:多人同时用时,每一轮
都比上一轮慢(第十四节那条爬坡);单人聊到 65,536 个 token 会撞模型的记忆上限,撞上
之后会发生什么,从来没人验证过。老办法是"写满了当场换新本子":用户提问的那一刻删掉
旧对话、重建新对话,那一轮要多等 3.70 倍。

**现在的做法:后台先备好新本子,到点只切一下。** 四步:

1. **数着。** 系统里有两个模型,各有一个会写满的记忆本 —— thinker(想:看画面、写回答)
   的本子主要被**画面**写满;talker(说:把回答变成语音)的本子被**它自己说过的话**写满。
   两个本子写满的速度没有关系(画面多话少,前者先满;画面静话多,后者先满),所以各盯
   各的,谁过警戒线都启动准备。
2. **备新本。** 趁用户不说话的空隙,悄悄开一个新对话,把"最近几轮的问答文字"提前塞进去
   读完,放着待命。旧对话照常服务,用户毫无感觉。备的过程中 talker 会对着这段文字咕哝一小
   段废话语音,系统**等它咕哝完、把废话扔掉,才算备好** —— 这一条是拿实测教训换来的,见下。
3. **换。** 用户下次提问的瞬间,把问题发给新对话。新对话早就读完准备好了,所以这一轮和
   平常一样快。备新本期间又聊过的话,文字也随这一轮补进去,一句不丢。
4. **删。** 旧对话等这一轮说完话再删,腾出显存。特意等说完:删除动作不和正在进行的对话
   抢跑(老办法里睡 1 秒防的就是这个,现在用先后顺序防)。

换完之后,新对话里有:最近几轮的**文字** + **当下的画面**。旧画面没有了 —— 这是每次
压缩付的代价。实测这个代价可控:文字扛住了 3/3 的内容回忆("我叫什么""我喜欢什么颜色"
换本后全答对),答不上的只有旧画面里的视觉细节。

**什么时候触发。** 两个本子各有两条线,默认配置的具体数字:

| 本子 | 准备线(过线就开始备新本) | 强制线(新本一直备不出来才走到:当场换,慢 3.70 倍) |
|---|---|---|
| thinker 的(画面为主) | 49,152(= 模型上限 65,536 的 75%) | 60,293(= 上限的 92%,保证永远撞不到 65,536) |
| talker 的(语音为主) | 38,250(= 45,000 的 85%) | 45,000(这面墙撞上引擎会死,不能赌,到线立刻换) |

这条线**不分单人多人,统一按模型上限的 75%**(潇的定案):压缩只负责一件事 —— 寿命,
让会话永远撞不到 65,536;多人场景下历史变厚带来的延迟,是调度要解决的另一个问题,不让
压缩兼职。统一政策带来一条硬容量规矩:**并发人数 ≤ 记忆池 ÷ 触发线**(这张卡:73.2 万 ÷
49,152 ≈ 14,留余量取 13)—— 超过它,池子会在触发线之前耗尽,见下面 16 人的实测死亡。

**实验配置(下面的数字在什么条件下成立)。** 一张 96 GB 的卡(RTX PRO 6000
Blackwell),三个模型阶段同卡:thinker 用 FP8 权重 + FP8 记忆(KV),talker 和波形级保持
bf16。显存怎么分:thinker 拿 70%(权重 31.4 GB + 记忆池 33.5 GB),talker 10%,波形 8%,
剩下是运行时缓冲的余量。**记忆池共 731,904 个 token,全场会话共享,启动时定死。**
并发名额 20;每帧缩到 640×352(220 个 token);内容是最费的手持高动态;每 0.5 秒送一帧,
过滤后每轮实际进 4–5 帧;问题短句、回答一两句。

**为什么这张卡最多撑 13 个用户。** 统一 75% 政策下,每个会话的记忆最多长到 49,152 个
token 才换本 —— 也就是说每人要在池子里占住一块最大 49,152 的地盘:

| 并发 | 峰值合计占池 | 结果 |
|---|---|---|
| 13 人 | 639k = **87%** | 实测通过(剩下的 13% 给换本时新旧本短暂并存、以及日常周转) |
| 14–15 人 | 94–101% | 理论临界,未实测 |
| 16 人 | 786k = **107%** | 实测死亡 —— 池子在人均 ~45k 时(合计 98%)就见底了,谁也没活到换本线 |

要撑更多人,三条路:换更大记忆池的卡、把触发线调低(等于回到延迟旋钮的用法,非当前政策)、
或者做真正释放显存的细粒度删除(第二把刀,架子上)。

**测出来什么样(三个规模,同一条 75% 线)。**

**单人 × 48 轮:** 全程 p50 371 / p95 409 毫秒,一条直线。第 34 轮 context 到 49,217,
自动触发;第 35 轮无感换本(409 毫秒,邻轮 373/365)。**第 44 轮起,会话活在了"原本的
坟墓"里** —— 不压缩的话,累计输入在第 ~43 轮就顶到 65,536,这是第一个活过那个点的会话。
换本后的记忆也验过:另一场把线压到 2,500、强制换两次本的冒烟里,"我叫什么""我喜欢什么
颜色"这类跨界问题 3/3 全对。

**13 人 × 48 轮(容量边界内,13 × 49,152 = 池子的 87%):** 624/624 零超时,13 次换本
全部无感,0 次退回老办法。曲线是教科书式的锯齿:

| 轮次 | 每轮 p50(毫秒) |
|---|---|
| 第 10 轮 | 821 |
| 第 26 轮(锯齿顶) | 2,689 |
| 第 30 轮(换本之后) | **815** |
| 第 38–48 轮 | 900–970,平 |

锯齿顶的 2.7 秒是 75% 政策在 13 人下的代价 —— 压缩管的是寿命,不管这个;把这个尖压下去
是调度(急活插队)的活,另一把刀。

**16 人 × 48 轮(容量边界外,16 × 49,152 = 池子的 107%):** **全员死亡,且死在触发线
之前一步。** 人均 ~45k 时(合计 = 池子的 98%)记忆池耗尽,所有会话卡在"分配不到显存块"
的等待里,回合信号永远不来,每人 3 次超时后被判弃;整场 0 次换本 —— 没有一个会话活到
49,152。死亡全程被第七节的自检探针实时转储,一条日志不缺。这场死亡就是上面那条容量
规矩的实测证明。

**本节所有场景的延迟汇总(TTFA,毫秒;都在统一 75% 线下,除最后一行)。**

| 场景 | p50 | p95 | **p99** | 最大 | 超 2 秒 | 形状 |
|---|---|---|---|---|---|---|
| 1 人 × 48 轮 | 371 | 409 | **437** | 437 | 0% | 平线 |
| 13 人 × 48 轮 | 968 | 2,915 | **3,537** | 3,885 | 14.6% | 锯齿(顶在第 22–29 轮) |
| 16 人 × 48 轮(超容量) | 1,161 | 3,765 | **5,347** | 5,915 | 26.0% | 死亡前存活轮的数字;第 24 轮起陆续回合丢失 |
| 对照:16 人不压缩(第十四节基线) | 1,378 | 4,477 | **5,368** | 10,194 | 38.4% | 只涨不回的爬坡 |
| 旁注:16 人 @ 16,000 手动线 | 871 | 1,320 | **1,860** | 6,498 | 1.0% | 低锯齿(非当前政策) |

*(旁注:压缩机制本身有能力兼任延迟管理 —— 16 人把线手动调到 16,000 时曾测得 p95
1,320 毫秒、40 轮全平、零死亡。按当前政策不这么用,记录在此只为标明机制的能力边界。)*

**踩过的三个坑。**

1. **备新本时不能让模型"只读不说"。** 那样 talker 收到的第一页是空的;等换本后第一轮
   真内容到达,页码按全文算、数据只有增量,对不上号,stage-1 引擎当场死。修:备本时让它
   读完后说两个字再停 —— talker 收到的第一页就和任何普通对话的第一页一模一样。
2. **"备好了"必须等废话语音放完再算数。** 同一个对话里的活是排队干的:切早了,用户的
   真话排在废话后面 —— 实测 640 轮里有 8 轮"文字 0.1 秒就到、声音等 6 到 26 秒",全部
   出在这。改成"废话放完才算备好"后,零再犯;等待全部发生在后台,旧对话照常服务。
3. **影子也占引擎的并发名额。** 名额要留余量(16 人配 20),抢不到名额的影子只是晚一轮
   换本,不是错误;但余量本身费显存 —— 配到 34 直接把波形那级挤到启动就崩("显存配额
   只管记忆池、不管运行时缓冲"这一课第三次应验)。

**开关**(都在 `session.config`,`context_compression_trigger_tokens` = 准备线,默认
自动取模型上限的 75%,传 0 关闭;`context_compression_target_tokens` = 换本时最多带走
多少内容,默认 16,384(见下一节:以前是纯文字 4,096);`context_compression_carry_frames`
= 换本时带不带画面,默认带,设 false 回到只带文字;`context_compression_warmup_timeout_s`
= 备新本最多等多久,默认 30 秒,超时放弃、退回老办法)。部署两条硬规矩:并发名额
`max_num_seqs` ≥ 会话数 + 影子余量;并发人数 ≤ 记忆池 ÷ 触发线。

---

## 十六、换本时把画面也带过去 —— 代价是多人变慢

*2026-08-03,提交 `4680cd57` / `14a34076`。*

**要解决的问题。** 上一节的换本只抄**文字**。文字够回答"我叫什么",但画面里的细节
(那人穿什么颜色、架子上放着什么)全丢 —— 这一点第九节早就量过:写文字小本,复述
8/8 对;画面细节 0/8。所以换一次本,模型就"失忆"一次,只是失的是眼睛不是耳朵。

**现在的做法:换本时带走一个"最近窗口"的原文,画面原样带上。** 具体是:最近几轮
连图带字整份塞进新本(每帧按 250 个 token 记账),再往前的轮次降级成纯文字垫底,
更早的整轮丢掉。窗口装满 16,384 个 token 为止。这正是 Gemini Live 文档里描述的形状
(掐掉最老的、留下的从一个完整用户轮开始、系统指令永远保留),我们只是把"留下的"
从纯文字换成原文。

**测出来什么样。**

单人 10 轮的对照实验最干净:前 4 轮镜头对着室内(一位穿深蓝上衣的人,架子上有个
奖杯),第 5 轮起镜头切成屏幕录像,然后在换本之后去问旧画面的细节 —— 文字记录里
从没提过这些细节:

| 换本后问 | 带画面(现在) | 只带文字(旧行为) |
|---|---|---|
| 那人上衣什么颜色 | "dark blue shirt" ✓ | "black and white patterned top" ✗(编的) |
| 架子上有什么 | "trophy on the shelf" ✓ | "silver award"(蒙对一半) |
| 我叫什么(文字对照) | ✓ | ✓ |
| 换本那一轮的延迟 | 451 毫秒 | 常态中位 396 毫秒 |

画面还活过了连续两次换本。

**单人 48 轮:免费。** p50 415 毫秒,全程平线(前 24 轮 414、后 24 轮 416),换本
那一轮 371 毫秒,种子里带了 119 帧。也就是说单人场景下"带画面"这件事不要钱。

**13 人 × 48 轮:要钱,而且不便宜。** 624/624 零超时、23 次换本全无感、零回退,但
延迟整体抬了一档:

| | 只带文字(4,096) | 带画面(16,384) |
|---|---|---|
| 完成 | 624/624 | 624/624 |
| p50 | 980 毫秒 | **1,355 毫秒** |
| p95 | 3,008 | **5,080** |
| p99 | 3,696 | **7,190** |
| 最大 | 5,953 | **11,088** |
| 超 2 秒 | 15.2% | **32.1%** |
| 锯齿顶(每轮 p50) | 3,110(第 25 轮) | **4,745**(第 27 轮) |
| 换本之后的平稳段 | 910 | **2,059** |

**为什么变贵,一句话:** 换本后每人的上下文从 ~4k 起步变成 ~14k 起步,而每帧的预填
开销是按"当前上下文有多厚"收费的(第十三节的单价表),所以连换本之后的平稳段都翻倍。
这不是缺陷,是"视觉记忆在多人场景下的标价"。把这个尖压下去仍旧是调度(急活插队)的活。

**三个按实测定下来的参数。**

1. **窗口 16,384,不是 32,768。** 32k 的种子实测中位 9.5 秒才备好,而 13 人挤在一起
   越线时,从准备线到强制线的 11,141 token 跑道只够每次 5.8 秒 —— 排在后面的会话会
   一边等一边长到 58,680(强制线 60,293),跑道快被吃光。窗口减半正好补上这个缺口。
   代价写在明处:窗口能罩住的画面从约 125 帧变约 62 帧。
2. **同时最多 2 个后台预热。** 池子撑不住第三个:13 人在触发线上已占池子 87%,两个
   16k 种子还能塞进剩下的 92,928,三个不行(实测两个在飞时已冲到池子的 ~110%)。
3. **抢不到名额要排队,不能"跳过、下个钩子再试"。** 原来的重试靠帧到达和回合边界触发,
   可堵车时这两样恰好慢到 1 秒一次 —— 实测 1–2 个名额空转约 10 秒而 9 个会话在等。改成
   每会话一个等待者、先过线先服务后,16 次排队全部拿到名额,零回退。

**修掉的两个引擎缺陷 + 一个仍开着的。**

1. **一个标记就崩。** 只带一个 `<|im_start|>` 标记的块传给说话模型时,`torch.nonzero(...)
   .squeeze()` 把 [1,1] 压成了标量,`torch.cat` 拒绝拼接 —— stage-1 当场死。改成
   `squeeze(-1)`(对任何数量都正确),并在那里打印标记数和首尾 token:这个形状以前
   无从得知。
2. **一段被拆开算,只发尾片。** 引擎负载重时会把一段的预填拆成几步做,而打包器每步
   都覆盖上一步的暂存,最后只把**最后一步**的行数打包发走 —— 丢掉的正是块开头的
   `<|im_end|>\n<|im_start|>user`。改成累加,ids 按累加后的行数取。
3. **仍开着:0.8% 的轮次仍会送出残片。** 最终 624 轮里有 5 次(拆段计数为 0,所以
   产生者不是拆段),说明还有另一条让"算出来的行数少于整段"的路径。崩溃已有防护,
   现在是**有日志的已知缺陷**,不再是静默错误。真正的修法是把载荷框架从"一步一包"
   改成"一段一包",属于上游改动。

**两条走死了的路,写下来免得重走。**

- **"已发给说话模型多少 token"这个水位线量不出段长。** 到达即预填的帧按设计根本不发
  载荷,它们的 token 会永远算在下一段头上 —— 实测报出的缺口正好是 1、2、4 帧。
- **不能扣着半段的载荷等它填齐。** 阶段之间是一步一个载荷按序对应的,扣一个,下一步
  的解码载荷就被当成这一段的预填读 —— `KeyError: 'prefill'`,引擎死。所以现在对半段
  是**报警**而不是硬修。

### 每人最多攒 8 帧 —— 一个参数买到的最大一笔改善

*潇的定案。两个客户端配置里 `max_frames` 从 256 改成 8。*

**为什么会攒。** 帧到了本该立刻预填(第十二节),但这条路在**一轮进行期间是整轮关闭**
的 —— 判据是"这一轮答完没有",不是"引擎忙不忙",因为帧和回合正文共用一个队列,插进去
会让回答的分段边界认错东西。备新本期间同样关闭(预填会落进马上要扔掉的旧记忆)。被拒
的帧不会被重试,就一直躺在缓冲区里,等下一轮**整批**交出去。13 人那场实测:**2,727 帧
走了预填,4,903 帧被拒 —— 64% 的帧压根没享受到这个优化**,而且失效的时机恰恰是最需要它
的拥堵时刻。

**为什么上限是 8。** 单人 47 轮回归出一轮的价钱:

```
TTFA ≈ 348 毫秒 + 59 毫秒 × 这一轮的帧数
```

(实测中位:1 帧 392、2 帧 426、3 帧 462、4 帧 521、7 帧 567、13 帧 876 毫秒。)地板
348 毫秒是不含帧的固定开销,59 毫秒是一帧的边际价;13 人时地板抬到 ~1,000 毫秒,一帧
按拥堵倍数约 150–180 毫秒 —— 用它反推那个 19 帧的尖:1,000 + 19×180 ≈ 4,400,实测
4,745,对得上。所以上限不是拍脑袋,是**"给帧的延迟预算 ÷ 一帧的价"**:8 帧 = 单人最多
472 毫秒、13 人最多约 1,400 毫秒。超出的**丢最老的一帧**(直播场景里新的才值钱)。

**效果(三档,全部带画面 + 上限 8)。**

| | 1 人 | 3 人 | 13 人 |
|---|---|---|---|
| 完成 | 48/48 | 144/144 | 624/624 |
| p50 | 409 毫秒 | 573 | 1,168 |
| p95 | 544 | 828 | 3,535 |
| 超 2 秒 | 0% | 1.4%(仅第 1 轮) | 18.1% |
| 用户间差距 | — | 1.09 倍 | 1.60 倍 |
| 换本看得出吗 | 看不出 | **看不出** | 看得出 |
| 全场丢帧 | 2 | 15 | 149 |

**上限前后对比:**

| | 1 人:无上限 → 上限 8 | 13 人:无上限 → 上限 8 |
|---|---|---|
| p50 | 415 → 409 | 1,355 → **1,168** |
| p95 | 876 → **544** | 5,080 → **3,535** |
| 最大 | 4,465 → **601** | 11,088 → 9,524 |
| 超 2 秒 | — | 32.1% → **18.1%** |
| 换本窗口尖 | 876(13 帧)→ **无凸起** | 4,745 → 4,041 |
| 换本后平稳段 | — | 2,059 → **1,165** |
| 用户间差距 | — | 2.06 → **1.60 倍** |
| 播放卡顿 p95 | — | 834 → **558 毫秒** |

**两点要读懂。** 一是**单人那两个尖(换本轮 876、第一轮 4,465)直接消失了**,而整场只丢
2 帧 —— 这两个尖本来就是"攒了一批帧再一次交账"。二是 13 人这边**尾巴和平稳段收益最大**
(p95 −30%、平稳段 −43%、公平性 2.06→1.60),而那个 4 秒的尖只降 15%:它主要不是帧堆积,
是 13 个人的种子预热和正文抢同一张卡 —— **这条把"尖归调度、堆积归配额"分得很干净**。

**所以到 3 人为止,"换本时把画面带过去"是免费的**(573 毫秒、换本无痕、公平性 1.09);
13 人开始付钱,付的主要是尾部,而尾部已经被一个参数砍掉三分之一。

**顺带说明两件事。** 上限是**每人一份**:缓冲区是每个会话自己的局部变量(存是各存),
但帧最终倒进同一个引擎抢同一份每步预算(算是抢算)—— 所以这个上限也是一个粗糙的
"按人配额"。丢帧计数打进了每轮的收尾日志(`frames_dropped=`):会丢输入的延迟控制,
不打出来就没人能审计。

---

## 十七、三种场景各能坐多少人 —— 天花板由摄像头决定

*2026-08-03,提交 `1210772d`。*

一句话结论:**同一台引擎,纯音频坐到 32 人还没到顶;基本不动的视频约 24 人;
高运动视频到 13 人已经明显变慢。三个顶的原因各不相同,没有一个是"模型算得不够快"。**

**量法。** 三个场景只差一个变量:摄像头。提问一律用文字(代替语音转写),回答一律
是语音,所以输出侧(说话)的负载三个场景完全一样;差别只在输入侧 ——
没有画面(`--content none`,本次新加)、基本不动的画面(talkinghead,相似帧过滤器
把它挡到只剩强制放行的 25%)、高运动画面(handheld,57% 的帧都算"新")。
每格 48 轮/人,和第十六节同规格。

### 场景一:纯音频 —— 32 人未见拐点,顶是配置不是算力

| 人数 | p50(毫秒) | p95 | 超 1 秒 | 思考端 p50 | 说话端 p50 | rtf p10 |
|---|---|---|---|---|---|---|
| 1 | 317 | 319 | 0% | 33 | 284 | 1.65 |
| 4 | 504 | 616 | 0% | 36 | 464 | 1.45 |
| 8 | 607 | 645 | 0% | 39 | 569 | 1.38 |
| 13 | 633 | 677 | 0% | 40 | 593 | 1.34 |
| 16 | 639 | 698 | 0% | 42 | 596 | 1.32 |
| 20 | 663 | 731 | 0% | 43 | 620 | 1.29 |
| 24 | 668 | 746 | 1.0%* | 45 | 622 | 1.29 |
| 28 | 690 | 762 | 0% | 47 | 641 | 1.25 |
| 32 | 702 | 798 | 0% | 48 | 652 | 1.23 |

\* 24 人那 1% 全部是引擎重启后的第 1 轮(重启后第一轮,内核还在现场编译的冷启动),不是并发效应。

三件事值得读懂:

1. **延迟几乎全是说话端的固定起步。** 思考端(出第一个字)从头到尾只要 33–48 毫秒;
   剩下的都是从字到声音的流水线起步,而它几乎不随人数变。
2. **唯一的台阶在 1→8 人之间,而且是两档价,不是斜坡。** 独奏 315 毫秒,合奏 585:
   4 人时两种轮次 44% 对 56%,落在两档之间的只有约 13% —— 台阶的本质是"这一刻
   有没有别人同时在生成语音"。8 人起基本全是合奏价,此后到 32 人只再涨约 15%
   (607 → 702)。
3. **真正在变薄的是 rtf 余量**(语音生成比播放快的倍数,掉到 1 就开始卡音):
   1.65 → 1.23。20 槽跑满后换 32 槽的配置重启(`deploy_mu_fp8_s32.yaml`),曲线照样平,
   所以纯音频先撞的是**槽位数(配置)**,再往后才是说话端的 rtf 墙 —— 按 13→32 人的
   斜率外推:余量掉穿 1.1 约在 50 人上下,真掉到 1(开始卡音)约在 70 人上下,
   **都是外推,没测过**。

### 追问:纯音频的瓶颈是不是显存带宽?—— 实测:不是,差得远

解码经典上吃显存带宽(每出一个 token 都要把权重从显存读一遍),所以这个怀疑很自然。
验证办法:重跑纯音频 8 / 16 / 32 人,旁边按秒采样三样东西 —— DRAMA(显存接口真正在
传数据的时间占比)、sm%(有计算核在跑的时间占比)、以及按进程拆的占用(pmon)。
不做锁频实验:卡和别人共享。

| 纯音频 | 空载 | 8 人 | 16 人 | 32 人 |
|---|---|---|---|---|
| DRAMA 中位 / p90 | 0 | 0.07 / 0.10 | 0.10 / 0.15 | **0.17 / 0.22** |
| sm% 中位 / p90 | 0 | 27 / 63 | 34 / 82 | **58 / 90** |
| 功耗(瓦,上限 600) | 81 | 119 | 138 | 184 |

三个判断:

1. **带宽离饱和很远。** 32 人(视频容量的 2.5 倍)才用掉 17%,随人数近似线性
   (0.07→0.10→0.17),照这个斜率 100 人也才一半 —— 而 rtf 余量的墙外推在
   50–70 人就先到了。**显存带宽不会是这台引擎任何可达人数下的瓶颈。**
2. **形态是"很多小核",不是"读不动数据"。** sm% 的 p90 已到 90(几乎总有核在跑),
   但真正的核占用率(SMACT)只有 0.23 —— 跑着的核只用了一小部分计算单元。这是
   调度 / 小批量形态,和"合奏价是离散台阶"互相印证:贵在排队合批,不在资源耗尽。
3. **按进程拆,大头确实是说话端,但思考端在追上来。** 32 人时中位 sm%/mem%:
   talker 29 / 6,thinker 15 / 4,code2wav 4 / 0。thinker 虽然"也源源不断",但它
   每轮只产十几个字的 token、几百毫秒就写完便回到等待;talker 要按 87 个
   codec token / 每秒音频(25 个一块、288 毫秒一块)跟完整个交付窗。不过从
   16 人到 32 人,thinker 的份额从 6% 涨到 15%,人再往上思考端会继续变重。

一处诚实的修正:动手前按"talker 每步全读 6.26 GiB 权重"估算,预测 32 人 DRAMA
在 0.4–0.6,实测只有 0.17 —— talker 进程有核在跑的时间只有 29%,说明语音生成是
一阵一阵的(按块的节奏),不是贴着播放速度连续解码。估算高了三倍,以实测为准。

### 场景二:视频基本不动 —— 24 人可用;但长会话曾经 20 人集体卡死

先看 48 轮的斜坡(24 和 28 人用了降低的压缩触发线,原因见下):

| 人数 | 触发线 | p50 | p95 | 超 1 秒 | 超 2 秒 | 思考端 p50 | rtf p10 |
|---|---|---|---|---|---|---|---|
| 13 | 默认 | 758 | 988 | 4.0% | 0% | 154 | 1.30 |
| 16 | 默认 | 802 | 1,071 | 12.2% | 0% | 177 | 1.27 |
| 20 | 默认 | 873 | 1,219 | 25.3% | 0% | 210 | 1.22 |
| 24 | 20,480 | 991 | 1,755 | 48.7% | 2.4% | 265 | 1.14 |
| 28 | 16,384 | 1,141 | 2,252 | 68.8% | 9.6% | 312 | 1.06 |

爬升的来源很清楚:画面不动,过滤器也要强制放行四分之一的帧,每轮历史照样厚
~850 个 token,而帧价与历史厚度成正比(第十六节)。思考端从 154 涨到 312;
说话端也被挤(154→312 的嚼帧工作和语音生成抢同一张卡的算力)。按"p50 别过
1 秒、rtf 余量别掉穿 1.1"的标准,**这个场景的实用上限在 24 人附近**。

**长会话的集体卡死,和一个参数的救活。** 72 轮 × 20 人、全部默认值:20 个用户
**全部**超时(首个超时落在第 46–62 轮,其中 17 人在 46–55),全部 20 个会话被标记
"零输出卡死"(思考、说话两个阶段各记一条,共 40 条)。引擎日志把死法写得很直白:

    waiting=0 skipped_waiting=20 running=0

每个会话都握着自己的 KV 缓存(模型为已读内容存下的中间结果,常驻显存)等下一块显存,池子(这次 20 槽 boot 上是 731,904 个
token)已经被 20 × ~37k 占满,谁也让不出来,谁也进不去。**这不是崩溃,是活锁**:
抢占计数全程为 0(引擎每条请求转储里的 preemptions 字段都是 0),引擎循环一次次停车,直到有用户超时断开、显存被动释放,
幸存者才动了起来。

最关键的一点:**压缩从头到尾没有触发**。这 20 个会话全都挂好了压缩的钩子,但触发线是
**每会话** 49,152 个 token,而池子 ÷ 20 人 = 36,595 —— **池子在触发线之前 25% 就
满了**。触发条件只看单人,池子却是大家共享的。

救法就是庄潇提的那句"适当减少 context":把触发线降到池 ÷ 人数之下
(触发 24,576、窗口 8,192),同样的 72 轮 × 20 人:

| | 默认触发线 | 触发线降到 24,576 |
|---|---|---|
| 完成 | 1,007(60 次超时,373 轮放弃) | **1,440 / 1,440** |
| 零输出卡死(日志条数) | 40 | **0** |
| 隐形换本 | 0 | 40 次(每人约 2 次) |
| p50 | 904(完成的轮次) | 927 |

代价是 p50 高 23 毫秒(若只与死亡组卡死前的轮次 892 相比,是 35 毫秒)。曲线上能直接看到换本把"变厚"打回去:37–40 轮爬到
1,150 之后回落到 ~900,再没有失控上涨。

顺理成章的下一步(**还没写**):触发线自动取
min(0.75 × 上下文上限, 0.75 × 池 ÷ 活跃会话数) —— 一行公式,证据就是上面那张表。

### 场景三:视频高运动 —— 13 人,瓶颈是帧 prefill,即把新画面读进模型那一步(第十六节已量)

### 三场景同框(13 人,同规格)

| 13 人 | 纯音频 | 视频不动 | 视频高运动 |
|---|---|---|---|
| p50 | 633 | 758 | 1,168 |
| p95 | 677 | 988 | 3,535 |
| 超 1 秒 | 0% | 4.0% | 63.6% |
| 思考端 p50 | 40 | 154 | 416 |
| 说话端 p50 | 593 | 608 | 724 |

读法:纯音频和不动视频的说话端几乎一样(593 / 608),高运动会把说话端也挤高两成
(724);**但三列的主要差别仍在思考端(40 / 154 / 416)** —— 摄像头拍什么,决定
思考端每轮要嚼多少帧,也决定这台引擎是轻载还是重载。

### 测量期间顺带修正的

- mu_bench 的引擎日志路径指着 08-02 之后就不再增长的旧文件,所以此前所有格子的
  引擎侧探针都在数一个空切片(客户端指标不受影响)。现在用 `MU_ENGINE_LOG` 指向
  在跑的 boot 日志,探针关键词也换成了代码真实打的词。
- `counter_leak_clamped` 守卫在换本压力下偶发触发(单格最多 29 次),全部被钳住、
  零后果 —— 但泄漏的源头仍在上游,没修。

---

## 十八、第一面墙推掉了 —— 给说话端上 CUDA graph

*2026-08-03,提交 `1210772d` 之后的配置实验;一行改动。*

第十七节的诊断说:纯音频的第一面墙是调度 —— talker 每步几百个小内核逐个发射
(`enforce_eager`),有核在跑的时间只有 29%。诊断对不对,一试便知:把 stage-1 的
eager 关掉,让 vLLM 把 talker 的一步预先录成一张图,每步只发射一次
(`deploy_mu_fp8_s32_graph.yaml`,与 32 槽配置只差这一行)。code2wav **故意没动**:
一次只改一个变量。

**先过正确性,再谈速度。** 文字、音频时长、块节奏、全部探针正常;stage-1 的 KV 池
一个 token 都没缩(123,040);graph 只多占 ~200 MiB;启动多花 55 秒(录制是一次性
成本)。四段语音样本(数数、绕口令等)存在 `/data/zx/results/graph_ab/`,**最终
判决留给人耳** —— 文字闸门看不见说话端的毛病。

**结果(与 eager 同规格同协议):**

| 纯音频 | eager | CUDA graph |
|---|---|---|
| 独奏价(1 人 p50) | 317 毫秒 | **145** |
| 合奏价(32 人 p50) | 702 | **367** |
| 32 人 p95 | 798 | **497** |
| 说话端 p50(32 人) | 652 | **303** |
| rtf p50 / p10(32 人) | 1.42 / 1.23 | **3.62 / 2.65** |
| 播放卡顿 p95(32 人) | 268 毫秒 | **19** |

| 视频 13 人 | eager | CUDA graph |
|---|---|---|
| 不动:p50 / 超 1 秒 | 758 / 4.0% | **367 / 0.2%** |
| 高运动:p50 / 超 1 秒 / 超 2 秒 | 1,168 / 63.6% / 18.1% | **749 / 31.1% / 11.9%** |
| 高运动:p95 | 3,535 | 3,986(没变好) |

**四点解读。**

1. **两个价都腰斩。** 32 个人合奏的价(367)只比原来一个人独奏(317)贵 50 毫秒;
   说话端固定起步从 ~600 毫秒掉到 ~300。
2. **上一节外推的 50–70 人调度墙被推出了可测范围**:32 人的 rtf 余量从 1.23 涨到
   2.65。要找到新的墙,得先把槽位再加大 —— 那是下一次的事。
3. **负载会自己加强,这是封闭回路的性质。** 延迟砍半 → 每轮循环变短 → 同样 32 人
   每秒多产 26% 的轮次,所以占空比全线**上涨**(sm 58→93、DRAMA 0.17→0.27、功耗
   184→226 瓦):卡在干更多的活,不是干得更费劲。附带一个机制:步变快 → 同时在
   说话的流变少 → 合批变薄 → 每个 token 摊到的权重读变多 —— **拿带宽换延迟**,
   在带宽只用三成的卡上,这笔交易很划算。
4. **高运动视频的尾巴纹丝不动**(p95 3,986),因为它根本不在说话端:那是 thinker
   侧的帧 prefill 和换本窗抢卡 —— 第十七节排好的下一面墙(插队调度)的地盘。
   中位数的改善(−36%)全部来自说话端那一半。

### 新墙现身:48 人干净,64 人撞上说话端的 KV 池

墙推掉之后,把槽位加到 64(`deploy_mu_fp8_s64_graph.yaml`,只改三处
`max_num_seqs`),继续纯音频梯子:

| 纯音频(graph,64 槽) | 48 人 | 64 人 |
|---|---|---|
| 完成 | **2,304 / 2,304** | 2,585,+15 超时,472 轮放弃,112 个连接错误 |
| p50 / p95 | 484 / 644 | 596 / 830,**p99 = 57,028** |
| rtf 余量 p10 | 1.97 | 1.46 |
| 零输出卡死 | 0 | 125 条 |
| 非零抢占计数 | 0 | **恰好 64 条,全在 stage-1,每会话一次** |
| DRAMA / SMACT / 功耗 | 0.31 / 0.45 / 241 瓦 | 0.31 / 0.44 / 236 瓦 |

(48 人有 26 个超 1 秒的轮,全部是重启后第 1 轮的冷启动。)

死因一行就能读出来:64 人时**带宽和算力比 48 人还闲**,但 64 个 talker 会话
每个恰好被抢占一次 —— 说话端自己的 KV 池(116,384 个 token,只有思考端池子的
1/6)装不下了,被抢占的请求要整段重算,一步慢步步慢,最后服务器把 56 个用户
按"空闲超时"踢下线。**又是一面容量墙,而且和第十七节那面是同一个家族**:
思考端的池子由压缩触发线守着(修法是触发线看池),说话端的池子今天之前
没人守。

所以纯音频的容量结论修正为:**这张卡、这份配置,~48 人干净;48–64 之间撞
说话端 KV 池。** 想再往上,选项按代价排:给 stage-1 多分显存(0.10 → 0.15,
卡上还有 ~5.5 GB 空闲)、按池子做准入控制(和触发线公式同族)、或给 talker
的 KV 上 FP8(质量敏感,动它之前要先过听感闸门)。

### 加显存、配守卫 —— 两个修各买到了什么

按"1 和 2 都做"推进:显存份额向说话端挪(stage-1 0.10 → 0.15,池子 116,384 →
365,344,×3.1;思考端让出 8%),再给说话端配上和压缩触发线同族的守卫 ——
每轮把换请求的阈值压到 0.75 × 池 ÷ 活跃会话数 之下,低于换本地板(2,048)的
新会话在入口直接拒绝(`stage1_kv_pool_tokens`,默认关)。计数器只在一个包装
函数里加减 —— 第十三节那次泄漏的教训。

64 人 × 48 轮,三格对照:

| | A 只加显存 | B 显存+守卫(真实池值) | C 守卫开,池子谎报成 116k |
|---|---|---|---|
| 完成 | **3,072/3,072** | **3,072/3,072** | 2,016(=42 人全程)+ 22 人入口拒绝 |
| p50 / p95 / p99 | 614 / 804 / 966 | 614 / 921 / 1,951 | 452 / 1,449 / 1,607 |
| 换本次数 | 0 | 104 | 140 |
| 抢占 / 卡死 / 超时 | 0 / 0 / 0 | 0 / 0 / 0 | **0 / 0 / 0** |

三句话读完:

1. **显存单独就把 48 轮视界内的 64 人救活了** —— 而且推翻了赛前的账:总需求
   ~512k 按"全量常驻"算仍超 365k,实际没爆,说明说话端的驻留比全量松得多
   (这也解释了此前 32 人在 123k 池上为何无事)。账错了就记下来:**池子的
   真实驻留模型还没量过,守卫的 0.75 系数因此偏保守。**
2. **守卫在不紧的池子上会买贵保险**(B 的 p99 翻倍):按悲观模型 ~26 轮就
   开始换本。视界有限时可以只开准入、不压阈值;要跑无限长会话才值得全开。
3. **守卫在真正稀缺时完胜**(C 对照昨天同条件的死法):64 人冲 116k 池,
   昨天是全体抢占、p99 57 秒、56 人被踢;今天是 42 人干净服务(p99 1,607、
   超 2 秒 0%)、22 人门口拒绝、零抢占零卡死。**崩溃变成了容量边界。**

### 一路加到 128 人:墙在 ~90 人,按住它的还是时间线

守卫关掉、槽位开到 128,纯音频阶梯 80 → 96 → 112 → 128(每格 48 轮):

| 人数 | p50 | rtf 余量 p50 / p10 | 卡顿 p95 | 超时/抢占/卡死 |
|---|---|---|---|---|
| 80 | 756 | 1.45 / 1.16 | 353 | 1 / 0 / 1 |
| 96 | 909 | 1.17 / **0.88** | 747 | 0 / 0 / 0 |
| 112 | 1,130 | **0.91** / 0.76 | 1,175 | 0 / 0 / 0 |
| 128 | 1,342 | 0.75 / 0.63 | **1,805(最大 8.6 秒)** | 0 / 0 / 0 |

出事时的资源指纹(96–128 人):sm% 钉死在 95–98,SMACT 只有 0.54–0.59,
DRAMA 0.39–0.43,功耗 276–294 / 600 瓦。

三个结论:

1. **破的方式不是首响变慢,是中途断气。** TTFA 的 p99 到 128 人也才 1.7 秒;
   真正爆的是 rtf —— ~90 人处生成速度掉到播放速度以下,句子说一半停 1–2 秒。
   预缓冲保得住"开口",保不住"说完"。
2. **按住它的仍然是时间线,不是硅片。** 时间轴 95% 被占满的同时,计算单元还闲
   四成、带宽闲六成、功耗只到一半 —— 和上 CUDA graph 之前是同一种病,只是墙从
   ~50–70 人搬到了 ~90。剩下的组织开销名单:code2wav 还是 eager(和 talker
   当初同款)、stage-1 的 async_scheduling 还关着、三阶段倒手。
3. **两个内存池全程旁观**(零抢占、零卡死、零超时;思考端 128 × ~5k ≈ 池顶也
   没咬)—— 128 人是平滑地"越来越喘",不是崩,这正是想要的退化形态。

纯音频容量的最终账(这张卡、这份配置):**~48 人富余,~80 人可用,~90 人
开始断气,128 人人人都喘 —— 全程无一次崩溃。**

---

## 十九、三扇锁着的门,一扇一把钥匙——视频路径把优化全领回来了

*2026-08-08,提交 `d8ccfd48`、`baf0e711`、`9a6a0908`(外加合并项目的 `eea63d41`)。第十八节用 CUDA graphs 把语音价格砍半,后来 async 调度又买到 +51%——但只有音频领到了钱。三个缺陷把这些红利全部挡在视频门外。今晚三扇门都开了。*

**第一扇:声码器 CUDA graphs 一遇视频就崩。** 崩溃报错(`split_with_sizes 期望 88,拿到 81`)看着像声码器批组装处理不了混合块长。真凶在两层之上的记账错位:开图后,runner 把扁平 token 流垫到捕获桶(81→88,永远垫到 8 的倍数),而每请求的计数表没垫。音频从不崩,是因为整齐的 16-token 块碰巧总落在桶尺寸上。修法:切分前按声明总长裁掉垫尾;若流比声明还短则响亮报错。在当年杀死引擎的原格上验证:24 视频用户,96/96 轮。

**第二扇:async 调度让 talker 在任何浏览器视频通话开始一秒内暴毙。** async 下引擎在第 N 步的结束名单出来前就预排了 N+1 步,留一个 −1 占位等上一步的采样回填。视频会话会产生"只有画面没有提问"的追加,其 talker 段被边界封顶:**第一采样步就结束**,没人回填占位——−1 径直走进码本嵌入表(词表 3072),device assert。音频从不产生这种段,所以音频阶梯绿了一整夜、一通真人视频电话一秒即死。诊断守卫当场抓获赃值(`values=[-1]`);只在嵌入处钳位还不够——MTP 码预测器吃的是同一批 id。修法:在 runner 输入咽喉处一次性钳掉负数(对文本级是空操作),下游全体得救。用浏览器配方复现器验证:引擎从"1 轮即死"到 6/6 轮、健康 200。

**第三扇:合并语音对跑视频冰川级卡死**(13 人、十五分钟、零轮完成、无报错)。没有再诊断——直接在今晚的代码上**重测**:52/52 轮、p50 483 毫秒。病根被今晚的某个修复顺带清了(最可能是第二扇的钳位清掉了边界段病理,其次是 MoE 工作区守卫);归因诚实地说不清,复现诚实地消失了。

**解锁后的配置量到什么**(async + talker 图 + 声码器图,视频第一次跑全套):

| 格子 | 之前最好 | 今晚 | 变化 |
|---|---|---|---|
| 视频 13 人 p50 | 749 毫秒 | **457 毫秒** | −39% |
| 视频 13 人超 1 秒 | 31.1% | **11.5%** | 尾巴砍掉三分之二 |
| 视频 13 人 p95 | ~3,986 毫秒 | **2,200 毫秒** | −45% |
| 音频 32 人 p50 | 307 毫秒 | **297 毫秒** | 历史最好 |
| async 下浏览器视频通话 | 约 1 秒暴毙 | 4/4 轮 | 已修 |

180 轮、零超时、五项质量探针全零,分进程和合并进程两种启动都验过。

**今晚新发现、仍未结案:** stage 1 确定性地把部分段发成 16k+8 token 的尾巴(半个编解码帧;每轮基准恒 44 次,至少从 08-04 起就在)。这些块的尾帧解码出来是乱码。属陈年旧疾、影响有界、现已响亮记录——揪生产者的活排进了队列。

## 二十、三个引擎一个进程——平台落地,王座仍归 GIL

*2026-08-08,提交 `f2b1edeb`、`5aa565df`、`6221aa51`;设计文档在 `benchmarks/live_agent/design/TRI_COLOCATION.md`。*

第十九节清完了视频路径的欠账;这一节把 **thinker 并进了 talker 和声码器已经同住的那个进程**——三级共享一个 CUDA 上下文,这是"一个调度器端着整条时间线"(混合租户章的平台)的物理前提。

**先得为真的两件事。** 其一,显存记账:按进程号读显存会把同屋所有引擎都算到自己头上,第二个带 KV 的引擎的池子会被算成零。合并模式现改用逐引擎画像回退;实测同一进程里 talker 池 392,720、thinker 池 681,968——都没缺斤短两。其二,MoE 草稿纸:vllm 的工作区管理器把**同一块**缓冲发给每个来要的人,一个 MoE 引擎时安全,两个就是无声的数值腐蚀。现按各引擎的私有 CUDA 流分本(实测四本,thinker 那本 512 MB)。

**首次点火即成**——155 秒,一个 93.9 GB 的进程,两位客人按序握手(父进程按子进程建核的**同一份清单顺序**等待,顺序错位是无声挂死,所以两边由同一列表派生)。

**记分板诚实且喜忧参半。** 正确性:全绿(音频 32 人、手持视频 13 人、浏览器配方、五探针归零,全程)。性能对分进程:音频 p50 349 对 297、尾部差三倍;视频 p50 648 对 457——但把载荷快照改成**显存内克隆直运**(不再过锁页主存)把视频尾巴买了回来:超秒率 40% → 17.3%,p95 1702 → 1363。那笔著名的 291 MB 拷贝原来在研究指认的位置**上游被付了两次**:一次在异步输出快照里(全合并下已拆除),一次在 forward 期间的预暂存里——而后者被前缀缓存的池化器**硬性要求留在 CPU**,重新设计它的供给就是下一个挖掘点。

**判词。** 今天要伺候用户,分进程 async 配置仍坐王座;剩余差距是 GIL(三条 Python 忙循环、约 20 条线程、一个解释器)——语音对当年撞的同一堵结构墙,而它当年的三副解药三合一如今全都带着。把引擎循环搬出 Python 是论文级的活。三合一**今天**交付的是研究平台:一个进程、统一视野、分家的草稿纸、诚实的逐引擎预算——混合租户实验从此有了自己的房间。

## 二十一、多用户的 p99 慢在哪——慢在说话端,不在思考端

*2026-08-08,提交 `ef30abf9`;数据在 `/data/zx/results/p99aud_none_u*`,
分析脚本 `benchmarks/live_agent/analysis/p99_attribution.py`。*

问题:纯音频用户,每人一条长会话。用户越多,最慢的那批回合(p99——
一百个回合里最慢的那一个)越慢。慢在哪一步?

**怎么测。** 六格:1、4、8、16、24、32 个用户,每人一条 30 回合的
session 长会话,每格前重启引擎,两进程默认配置。新工具:压测器给每个
回合盖了绝对时间戳,于是任何一个回合都能回答——它到达那一刻,有几个
别人的回合还在等第一声、有几个正在放音频。这把"我在排队"和"大家都
变慢了"分开,光看百分位数是分不开的。

**第一个发现:吓人的 p99 是开机假象。** 整个梯子里所有超过 800 毫秒的
回合——六格共 32 个——全部是第 1 回合,而且全落在引擎开机后头 2.6 秒
之内。冷引擎的第一问在思考端要付约 3 秒;跟在它后面到的人排队,越晚到
付得越少(32 用户格:第 0 秒到的付 2,961 毫秒,第 2.3 秒到的只付
682 毫秒)。同一引擎上的第二条会话完全不慢。这是每次开机付一次的暂态,
不是规律——不剔除它,p99 甚至不随用户数单调(24 用户"赢"了 16 用户),
它就是这么露馅的。开机时打一发热身问题就能抹掉它。

**稳态(第 2 回合起):1 到 32 个用户,p99 涨 3 倍,涨幅约 85% 在说话
端。** 每个回合拆两段:思考端=提问到第一个字;说话端=第一个字到第一声
(talker 出第一批语音码,加声码器出第一块波形)。尾巴回合超出中位数的
部分,先在回合内拆、再取平均:

| 用户数 | 回合数 | TTFA p50 | p95 | p99 | 思考端 p50/p99 | 说话端 p50/p99 | 尾巴超额中说话端占比 |
|-----:|-----:|--------:|----:|----:|--------------:|--------------:|------------------:|
| 1    | 58   | 130     | 140 | 146 | 30 / 33       | 99 / 115      | —(尾巴是噪声)      |
| 4    | 116  | 134     | 233 | 248 | 30 / 75       | 105 / 187     | 67%               |
| 8    | 232  | 142     | 258 | 291 | 32 / 71       | 110 / 233     | 81%               |
| 16   | 464  | 220     | 321 | 362 | 51 / 85       | 168 / 288     | 85%               |
| 24   | 696  | 262     | 365 | 403 | 55 / 95       | 203 / 327     | 88%               |
| 32   | 928  | 296     | 400 | 435 | 61 / 100      | 235 / 360     | 84%               |

思考端从头到尾都不是问题:到 32 个用户它的 p99 还压在 100 毫秒以内。
说话端翻了三倍。

**说话端为什么涨:你在等第一声的时候,它正在给别人放音频。** 32 用户
下,到达时没人在播的回合,说话端只花 158 毫秒;有一路在播,221 毫秒;
四路以上,约 260 毫秒,然后就饱和了——共批的代价是亚线性的,没有硬性
排队。尾巴回合恰恰是运气差的那批:它们到达时,在等第一声和在播音频的
人数都是平均值的两倍。

**两个洗清嫌疑的。** (1) 上下文变长无罪:同一格里,第 2-10、11-20、
21-30 回合的 p50 和 p99 都是平的(32 用户:p50 为 296/307/289 毫秒)
——长会话本身不花钱,和第十三节一致。(2) 这轮跑得干净:2,532 个回合
零超时、零换请求、零卡死;每格有 2-6 次泄漏被计数器护栏钳住,那是
第十二节已知的旧泄漏被拦住了,不是新毛病。

**这换来什么。** 压多用户 p99 的杠杆不在思考端、不在显存,而在说话端
内部的优先级:一个回合的"第一块音频"在和别人的"后续音频块"抢同一批
算力。后续块有余量(32 用户下,生产速度仍是播放速度的 5-10 倍),第一
块才是用户看得见的停顿。在说话两级里让"第一块优先"就是正面攻击,和
清单上已有的"语音优先于文字"是同一个形状。

## 二十二、给 talker 开 4k 滑窗——"只能 8 人"的显存墙是自己画的

*2026-08-08;配置 `benchmarks/live_agent/web_client/deploy_mu_fp8_s128_async_sw4k.yaml`,
数据在 `/data/zx/results/sw4k_verify_u{1,8}`,听感样本在
`/data/zx/results/sw4k_verify_u1/wavs/`。*

容量账里的问题:顶到 45k 换请求线时,一个纯音频用户在说话端 KV 池里
躺着 45,000 × 20 KB ≈ 0.88 GiB,7.49 GiB 的池子只装得下 8 个人。可
talker 根本不需要 45k 的历史——它的活儿是接着最近几句的语气往下说,
每次换请求都在证明这一点(整段语音历史扔掉、只用 8 轮文字重新播种,
没人听得出来)。那能不能让 talker 只保留 4,096 个 token 的注意力窗口,
而会话、换请求线、用户体感的 context 全都原样不动?

**能,而且就是 yaml 里的一段。** 不改一行模型代码:vLLM 的注意力层
会从引擎的 cache 配置里读一个模型级窗口,V1 的 KV 管理器对**正在生成**
的请求边走边把滑出窗口的块还回池子——块表按线性位置索引(还掉的位置
用空块占位),位置照常增长,M-RoPE 完全不受影响。只加在 stage 1:

```yaml
hf_overrides:
  talker_config:
    text_config:
      sliding_window: 4096
```

**值得记下来的坑:** 平铺写法 `hf_overrides: {sliding_window: 4096}`
会落在 Omni 外壳配置上,**静默失效**——不报错、不警告、不省显存。
必须嵌套到 `talker_config.text_config`,因为 stage 1 解析文本配置时
认的是那一层。我们先做了不开 GPU 的配置解析验证,才烧的机。

**开机凭据(同样的 7.49 GiB 池子,其他全部不变):**

| stage-1 启动日志行                    | 全注意力 | 4k 滑窗 |
|---------------------------------------|--------:|--------:|
| Maximum concurrency(65,536/请求)    | 5.99×   | **19.16×** |
| GPU KV cache size(token)            | 392,720 | 1,255,722 |
| thinker 池(未被牵连)                | 677,968 | 677,968 |

两条读数规则。第一,19.16× 是**保底值**:准入口径按"窗口 4,095 +
调度器在途余量 16,384(2 × 8,192 批预算)≈ 20,479 token/请求"算——
就算 19 个人同时都在最大批的 prefill 里,池子也兜得住。稳态下一条
会话只占 ~窗口那么多,所以现实上限是 392,720 物理 token ÷ ~4k ≈
**~90 人**,原来是 8。第二,"GPU KV cache size: 1,255,722" 是滑窗
口径下的**等效**容量,不是物理字节——池子物理上仍是 7.49 GiB、每
token 20 KB;拿两者相除得出 6.4 KB/token 是把这行读错了,不是 KV
变便宜了。

**跨过窗口边界的机制验证。** 单用户 40 轮一条会话;模型累计说话在
第 25 轮跨过 4,096 个 talker token:

- TTFA:第 2–20 轮(窗口未满)p50 129 ms,第 25–40 轮(窗口每步都
  在滑)132 ms。持平。
- 文字到第 40 轮仍然正确、无复读;每轮音频时长持平(前 1.85 秒/后
  2.02 秒);rtf 全程 9.1–11.7。
- 引擎坏探针全零;唯一 >1 秒的是开机第一问的冷启动(见二十一)。

8 用户共批冒烟(滑窗 × CUDA graphs × async 调度的相互作用,单用户
测不到):80/80 轮,p50 138.5 / p95 276.2 ms,对全注意力基线的
145.8 / 275.9——同一水平。`counter_leak_clamped: 4` 是第十二节的
护栏在钳已知泄漏,处于 2–6 的正常带内。

**没定论的:听起来怎么样。** 这个权重是全注意力训出来的;窗口一旦
滑动,最早那些 token——连同它们可能起的"锚"作用——就没了,文本大
模型在这种地方栽过跟头(attention-sink 的教训)。上面的数字说明机制
和延迟干净,音质只能靠耳朵。40 轮的 wav 都在盘上(边界是 t25;拿
t02–t05 对比 t25–t40),滑窗引擎也留着在跑,可以直接上网页实听。
4k 过不了耳朵的话,8k/16k 仍有 2.5–6 倍的容量收益。

**这项改动不改变的:** stage-1 的 65,536 数组硬墙和 45k 换请求节奏。
位置照常增长,只有 KV 驻留封了顶。滑窗把说话端 KV 池从"绑死容量的
那堵墙"变成旁观者——此后剩下的墙是座位数(max_num_seqs 128)和
二十一节那条说话端延迟缓坡。

**当晚补记——talker KV 降 FP8,份额挪给 thinker**
(`deploy_mu_sw4k_kvfp8.yaml`,数据 `/data/zx/results/sw4k_kvfp8_verify_u{1,8}`)。
又是两步纯账面的挪动,不碰算力:stage-1 加 `kv_cache_dtype: fp8`
(权重仍 bf16——talker 直接喂声码器,配置文件开头记的 FP8 权重之坑
在权重路径上),省出的份额给 thinker:0.67/0.15/0.08 →
**0.72/0.10/0.08**。潇要 thinker 0.80:在 0.90 的共卡红线内放不下——
talker 进程有 ~7 GB 刚性开销(权重+graphs),砍份额全砍在池子上,
0.08 时池子只剩 ~0.6 GiB(滑窗+FP8 也只有 ~16 人),比二十二节之前
还差。0.72 是 talker 还能干活的前提下 thinker 的上限。

开机凭据:thinker 池 31.04 → **35.78 GiB**(677,968 → 781,696 token,
+15%);talker 物理池 2.74 GiB、保底并发 **14.03×**——这行本身就是
FP8 生效的算术凭据:bf16 之下 2.74 GiB 只保得住 ~7×,14.03× 只有在
10 KB/token 时才成立(287k 物理 token → 4k 稳态窗口下 ~71 人)。
功能验证与二十二节同一套流程:u1×40,第 25 轮跨边界——TTFA p50 前
128 / 后 131 ms,每轮音频 1.96/1.95 秒,到 t40 无复读,探针全零;
u8 共批 80/80,p50 161.9 / p95 268.8(p95 是三种配置里最好的;p50
落在基线各用户 137–195 的散布带内,单格样本分不出真伪)。听感测试
现在叠了**两个**质量变量:`sw4k_verify_u1/wavs` 是 bf16+滑窗臂,
`sw4k_kvfp8_verify_u1/wavs` 是 FP8+滑窗臂——两臂都过了耳朵,才谈
把哪个设成默认。

## 二十三、音频什么时候 burst 过一秒——96 人尾巴先破,128 人中位数也破,全在说话端

*2026-08-08;梯子配置 `deploy_mu_sw4k_kvfp8_t15.yaml`(talker 份额回到
0.15,让池子在设计上不可能是凶手),数据
`/data/zx/results/burst_none_u{32,48,64,96,128}`,完整明细
`/data/zx/results/burst_attribution.json`。*

问题:在新默认栈(talker 4k 滑窗 + FP8 KV)上,纯音频用户加到多少,
延迟开始 burst 过 1 秒?机制是什么?设计上先做了一个排除:梯子用
0.15 份额跑——785k 个 FP8 token,128 人时每人 6.1k,比不封顶的 30
轮累积还多 ~30%——**池子耗尽被构造性地排除**,烧出来的只能是算力或
调度。

**曲线(稳态,turn ≥ 2;每格 30 轮长会话、新开引擎):**

| 用户 | 回合 | TTFA p50 | p95 | p99 | >1s 占比 | thinker p50/p99 | 说话端 p50/p99 | rtf p50/p10 | 播放卡顿 >0.3s |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 32  | 928   | 296 | 404 | 480 | 0 | 60 / 97 | 234 / 402 | 5.27 / 3.72 | 0% |
| 48  | 1,392 | 375 | 486 | 536 | 0 | 69 / 107 | 304 / 470 | 3.79 / 2.86 | 0% |
| 64  | 1,856 | 477 | 610 | 668 | 0 | 79 / 120 | 396 / 575 | 2.88 / 2.23 | 0% |
| 96  | 2,784 | 711 | 920 | **1,064** | 2.5 | 90 / 139 | 618 / 970 | 1.75 / 1.40 | 1.6% |
| 128 | 3,712 | 971 | 1,265 | 1,523 | **42.1** | 100 / 160 | 870 / 1,427 | 1.19 / 0.96 | 29.2% |

**答案一——burst 线。** 尾巴在 **96 人**首次破 1 秒(p99 1,064 ms,
2.5% 的回合超线);到 **128 人**已经不是尾巴了:42% 的回合超 1 秒,
中位数本身 971 ms。64 人是最后一个干干净净的格子(p99 668 ms,零回
合破秒)。卫生:10,752 个回合零超时、零换请求、零抢占——128 人那格
也是 3,840/3,840 全部完成。FP8 之前的栈 8 人就逻辑死亡;现在的栈平
滑退化到座位上限。

**答案二——慢在哪。** 全在说话端。破秒回合相对格子中位数的超出部分,
thinker/说话端 = -0.01/1.01(96 人)、0.02/0.99(128 人);thinker 的
p99 到 128 人也只有 160 ms。(不剔 turn-1 的原始表里 thinker p99 高达
2,389 ms——那还是二十一节的开机排水,128 个 turn-1 排一条队。照例剔
除。)

**答案三——机制是吞吐耗尽,不是事件。** 实时率(每秒墙钟时间产出的
音频秒数)平滑衰减:中位数 5.27 → 3.79 → 2.88 → 1.75 → 1.19。128 人
时 p10 跌破 1.0——十个回合有一个产得比放得慢,29% 的回合播放中途
停顿超过 0.3 秒。什么都没坏;就是说话两级的算力用光了,每个回合的
第一块音频排在所有人的后续块后面(128 人时尾部到达的回合,前面平均
压着 ~37 个等首音的回合)。每加一个用户,说话端中位数付 ~6.6 ms,
thinker 付 0.4 ms。

**这补齐了账本的哪一块。** 这张卡的纯音频容量故事现在测穿了:显存在
二十二节退出了瓶颈位,剩下的墙是说话端算力——尾巴 ~96 人、中位数
~128 人。二十一节那根杠杆(说话两级里第一块优先)在 96 人处有真实
空间(rtf 余量 1.75,意味着 43% 的产出时间是后续块可以让出来的),
128 人处已经没有(p10 rtf < 1:没有东西可重排了,只有加吞吐——共批
效率、声码器批处理、引擎循环/GIL 那几个挖掘点)。

**补记——"SM 爆了吗?"没有。128 人的天花板是软件,不是硅片。**
潇问饱和是不是真的 SM 饱和。nvidia-smi 的利用率答不了这个问题(它从
32 人起就读 96%——卡上几乎总有某个 kernel 在跑——而底下 rtf 从 5.27
塌到 1.19)。用 DCGM 对两档现场重跑(同一次开机,jkim 空闲:探针前
设备级 SMACT 0.00):

| 窗口(10 轮) | TTFA p50 | rtf p50 | SMACT | SMOCC | TENSO | DRAMA |
|---|---:|---:|---:|---:|---:|---:|
| 32 人  | 295 ms | 5.03 | 0.42 | 0.05 | 0.05 | 0.29 |
| 128 人 | 964 ms | 1.20 | **0.65** | **0.16** | 0.13 | **0.46** |

就在延迟 burst 的那个工作点上,SM 有 35% 的时间整体空转,warp 占用
率 16%,显存总线用了一半。用户 ×4(总音频产量约 ×4),SMACT 只涨
×1.55——共批摊得非常好,硬件离顶还远得很。把 rtf 钉在 ~1.19 的服务
速率天花板,是 kernel 之间的**软件路径**:Python 引擎循环和语音对
(合并进程)共享的 GIL、每步的启动/组织间隙、连接器搬运。和当年
talker 没上 graphs 时(32 人 SMACT 0.23)是同一个病,高一层复发。
推论:128 人以上的容量**不需要更多 FLOPs**——需要的是引擎循环/GIL
那几个挖掘点;硅片已经买单了,正闲着。

**补记之二——说话端内部:talker 和 code2wav 现在平分账单,一个天然
实验指认了"交接"本身。** 潇问说话端里到底是哪一半在挡路:生成语音
token(talker)还是 token 变波形(code2wav)。py-spy 在这台机器上被
权限拦住(ptrace_scope=1,无 sudo),合并进程里两级又分不开,所以
用十九节的老办法:同一份 t15 配置,`VLLM_OMNI_COLOCATE_STAGES=""`
拆成三进程再压 128 人,读每进程 sm%:

| 级(128 人,三进程) | sm% p50 | sm% p90 | mem% p50 |
|---|---:|---:|---:|
| thinker  | 37 | 58 | 16 |
| talker   | 27 | 48 | 12 |
| code2wav | 27 | 53 | 12 |

两个发现。其一,**code2wav 不再是十九节里 4% 的旁观者**(那是 32 人、
graphs 之前):它的活随音频产量线性涨(32→128 人约 ×4),而 graphs
把 talker 变便宜了,到 128 人两级 GPU 时间打平。谁也没饱和(设备级
SMACT 0.65 仍然成立)。其二,这次探针本身就是个天然实验:**同样的
活,从合并进程改排成三进程,rtf 从 1.06 掉到 0.85,破秒占比 65% →
90%——只是重排了"谁把东西递给谁",吞吐就没了两成**,GPU 数学一个字
没动。交接/组织层不只是余量所在,它自己就是一等一的开销项。(注意:
探针格比梯子跑得热——pmon 和观测负载——只比形状,别比绝对 p50。另外
thinker 的 GPU 份额随人数一直在涨:16/32/128 人时 6% → 15% → 37%,
但它的延迟贡献始终 p99 ≤160 ms——它共批摊得好。)对首音路径,两级
是串联的:第一块优先(任务 #19)要同时覆盖两级才动得了 TTFA。

## 二十四、Duplex 式喂入——会话全天候付费,电表分毫不差

*2026-08-09;服务端开关 `prefill_audio_on_arrival` + `audio_prefill_group_ms`
(video_stream_base.py),压测模式 `MU_DUPLEX=1`(mu_bench.py);数据在
`/data/zx/results/duplex_t{1,3b,4,5,6}*`。*

潇睡前的指令:让 Qwen 会话像 duplex 模型那样进食——音频按固定节拍进,
**没人说话就喂数字静音**——让负载带上 duplex 的定义性特征:占空比
100%,说不说话上下文都在涨。建成,计划内的测试全部通过。

**建了什么。** 一根管子的两头:

- *服务端*:`prefill_audio_on_arrival`——帧路径的音频镜像。到达的 PCM
  按 `audio_prefill_group_ms`(默认 480)分组,渲染成纯音频增量、削到
  只剩占位符段(削壳器本来就不挑模态)、打上"只预填"标记、追加进活
  请求。拒收条件和帧完全一致(回合进行中、shadow 预热中、队列非空、
  首块未发),软失败契约也一致:被拒的音频一字节不丢,留在缓冲里等
  回合时统一清扫。消费从头部取,留下的永远是最新的尾巴。
- *压测端*:`MU_DUPLEX=1`——每个模拟用户每 `MU_DUPLEX_CHUNK_MS`(默认
  100)发一块 `audio.chunk`,永不停:话语间隙发静音,每次提问前发
  `MU_DUPLEX_SPEAK_S`(默认 1.5)秒的语音样 PCM。duplex 客户端那只
  永不关闭的麦克风。

**电表分毫不差。** 每一轮实验里,引擎的 stop 数 = append 数 + 回合数,
精确到个位(71 = 65+6;537 = 492+45;989 = 909+80)——每笔 append 是
一次被调度、预填、停止的微生成,一笔都没漏账。静音进上下文的实测速率
**~18 token/秒**(每 500 ms 一组、9 个 token)。

**单用户:机制与压缩跨线。**

- u1 × 6 轮:65 笔 append,回答全对,去掉开机瞬态后 TTFA p50 147 ms。
- u1 × 45 轮、压缩触发线降到 4,096:**纯静默流自己把上下文推过线**
  (cum 4,104),shadow 预热就绪、第 34 轮换底——全程最差的一轮只有
  **171 ms**。隐形换底在 duplex 喂入下完整成立,十七节的机械一行没改。

**八用户:占空比 100% 的第一张实测价签。** 同一次开机,三臂,各 10 轮:

| 臂 | TTFA p50 | p95 | rtf p50 | append 数 |
|---|---:|---:|---:|---:|
| 对照(回合式喂入) | 181 | 254 | 8.84 | 0 |
| duplex,组 960 ms | 174 | 278 | **8.85** | 433 |
| duplex,组 480 ms | 183 | 351 | 7.61 | 909 |
| duplex,组 200 ms | 278 | 428 | 6.10 | 2,212 |

480 ms 组下,占空比在**中位数上免费**(+2 ms),在尾巴(p95 +97 ms)
和送话余量(rtf −14%)上付费;960 ms 组几乎全部买回,rtf 完全恢复;
而 200 ms 组(每秒 5 笔)是中位数自己开始掏钱的地方(+97 ms),rtf
让出 31%。三个点一个读法:二十三节那笔"组织税"变成了旋钮,按笔收费、
且**随笔数超线性**(碰撞会相互放大)。各臂零超时、零破秒、探针干净。
次日早上潇把 **200 ms 定为服务默认**(更细的 duplex 节拍,价格如上
表);容量类梯子想要粗粒度的价格,用 MU_SESSION_CFG_JSON 按格覆盖。

**这是干什么用的。** Qwen 栈从此能端到端模拟 SeedRealtime 类负载的资
源侧:每用户全天候 ~18–25 token/秒的恒速上下文增长、节拍化进食、组
大小可调、说话按占空比叠加,且全套已有仪器(逐轮打点、探针、池账本)
原地可用。决策侧(模型自己决定何时开口)仍由客户端触发——这正是合成
RT 租户的时钟,是设计而非缺憾。

**已知的边界,直说。** (1) 纯静默期里就绪的 shadow 要等下一个回合才
换底,预热期间 append 被拒——音频在缓冲里攒着、稍后清扫;对话节奏下
无害,若会话可能几分钟零回合需要再看。(2) 首回合前的积压(预热+错峰
窗口)会作为一大块音频搭首回合的车。(3) 每笔 append 有一个被丢弃的采
样 token,所以 append 频率本身也是一种上下文增长率:480 ms 组下 ~2
笔/秒即 ~2 个废 token/秒,叠在音频 token 之上。

**补记——第一次真人上浏览器,找到了压测找不到的缺陷。** 潇一试,
"要不想很久、要不几步就报错"。两个缺陷,一真一假:

1. *真的那个:* append 的打包(模板渲染+音频特征提取)是 ~400 ms 的
   CPU 活,第一版把它**内联在 websocket 接收循环里**——而那个循环同时
   扛着所有会话的消息接收和音频下发。浏览器每秒发 ~7.5 条音频消息外加
   摄像头帧,循环被打满,提问在一墙音频消息后面排了 ~5 秒队,客户端
   最终放弃(引擎侧记录请求 DROPPED)。压测没抓到它:没开帧、组更粗、
   客户端有耐心。修法:每会话一个**喂食任务**把 append 从接收路径上
   摘下来,打包放进工作线程(独立事件循环——打包是纯"请求→prompt"变
   换);接收处理器只管攒字节。线程归来后按"复查协议"处置认领的字节:
   期间没有回合 → 塞回缓冲头部(时序完好);有回合插入 → 丢弃这一组
   并计数(塞回去会把旧声音拼在新声音后面)。
2. *说谎的表:* turn 日志的 `first_text` 锚在**上一轮的结束时刻**——
   回合是唯一输入事件时没问题,duplex 喂入下永远有输入在路上,它读出
   来的是思考时间+说话窗+延迟(~6 秒),而客户端正确地量到 150 ms。
   已改锚到回合增量入队的时刻。

双修后复验(浏览器同款负载:帧+duplex 音频,单用户):服务端稳态回合
first_text 0.034–0.070 秒、首音 0.117–0.166 秒,客户端 p50 175 ms,
探针干净。

## 现在的状态

**能用的**

- 单用户、开着摄像头和麦克风、连续长时间对话
- 每轮只处理新画面;画面自动缩小;取帧节奏有上下界
- 对话**可以无限继续**(自动换请求,用户察觉不到)
- 撞上限前有预警,上限距离每轮可见
- 三类静默故障会自己报出来
- 念稿子那部分不再背着画面 —— **每轮要拿的东西少了 25 倍**
- **一个浏览器客户端**:自己的摄像头麦克风进去,它的声音出来,只转发一个端口

**验证过的**

- 40 轮 + 中途换 2 次:没崩、没卡。同设置**不换的话第 27 轮就结束**
- 50 轮不换请求:50 轮全部完成,收尾干净
- 长会话的记忆能力没有变差(**在不换请求的前提下**)
- 把画面从念稿子那部分撤掉后,**每一轮**都照样出声(两边各 5 轮)
- 新引擎上的浏览器链路端到端:**五轮全部出声,第一声 340–376 毫秒** —— 在不用浏览器的情况下验证的,所以说的是服务器

**明确还没做 / 还没测的**

- **换请求之后的记忆没测过。** 按原理,文字带过去了、画面没带过去,所以**预期**是"记得说过什么、忘掉更早看到什么" —— 但没测就是没测,不能当结论。
- **多用户测过了(第十四节),但还活不下来。** 等待随人数扩展得很好;可共享记忆池一满,会话就在可预测的轮数**死掉**。能改变这件事的两个修复 —— 被挂起也能活、按上下文增速而非人头放人 —— 只在纸面上,还没建。
- 有一个"自愈"补丁能把卡住的任务捞回来,但**为什么会卡成那样,原因还不知道**。
- 那个坏掉的计数,我们只是**绕开**了,**没有去上游修**。
- 画面从念稿子那部分撤掉之后,**没有人听过音频**,而且**对话现在能撑多久也没测**(见第十节)。
- **浏览器里每轮的音频被截断到 0.22 秒**(见第十一节)。链路是通的,声音还不行。已经用 A/B 排除是我们的改动,但原因还没找到。
- **新引擎上的数字不等于旧数字。** 513 毫秒和那次 40 轮滚动都是在旧引擎、另一份配置上量的;跨两边比较之前必须重跑。

---

## 附录:配置项与代码位置

给需要找代码的人。每节标题下面的斜体行就是该改动的日期和提交号。
代码都在本仓库的 `live-agent` 分支,共 26 个提交、8 个文件、约 1,700 行。

第十节是唯一**不在** `session.config` 里的开关 —— 它改的是"一个模型阶段交给另一个阶段什么",
比"单次对话的配置"能触及的层次更低,所以做成了环境变量:`VLLM_OMNI_TALKER_TEXT_ONLY`,
**默认开**,`=0` 恢复原来的行为。

其余的配置项都在 `session.config` 里,默认值:

`max_frame_width` / `max_frame_height` = 不缩放 · `frame_jpeg_quality` = 90 ·
`frame_filter_min_gap` / `max_gap` = 不限制 · `session_scoped_request` = 关 ·
`session_talker_token_budget` = 无 · `session_roll_at_talker_tokens` = 关 ·
`session_roll_history_turns` = 8 轮 · `session_roll_settle_s` = 1 秒 ·
`context_compression_trigger_tokens` = 自动(模型上限的 75%;0 = 关)·
`context_compression_target_tokens` = 4096 · `context_compression_warmup_timeout_s` = 30 秒
