# live_agent harness utilities

The canonical capacity runner is `../web_client/run_av_session_ladder.sh`.
It uses `gpu_sampler.py` from this directory. `ttfa_bench.py` remains a small
synthetic diagnostic client.

Other files here are historical experiment drivers. They are retained only
when an archived result still references them; they do not define the current
application architecture or capacity workload.
